# 武器图鉴系统设计文档

## 1. 目标与边界

武器图鉴是独立的局外系统，业务代码集中在 `project/weaponCompendium.js`。它只读取 `project/weapons.js` 的中央武器定义，通过背包公开 API 观察武器获得结果，不参与背包摆放、出售、合成、属性计算或战斗结算。武器卡片外观由全局共享的 `project/weaponCardRenderer.js` 负责，商店与图鉴不再各自维护一套卡片 UI。

系统提供：

- 局外永久解锁；
- 游戏过程中获得武器时立即永久解锁；
- 本局武器获得历史（包括之后卖出或被合成消耗的武器）；
- 击败 50 层魔王后，为通关瞬间仍在背包中的武器添加永久通关标记；
- 黑色未解锁剪影与信息脱敏；
- 按已解锁武器名称搜索，并可按武器类型、解锁状态和通关状态筛选；
- 数据化条件与数据化奖励动作；
- 标题界面的“武器图鉴”入口；
- 供事件或其他独立模块调用的公开 API。

## 2. 文件与接入点

| 文件 | 作用 |
| --- | --- |
| `project/weaponCompendium.js` | 图鉴配置、局外档案、本局历史、奖励、UI 和公开 API |
| `project/weaponCardRenderer.js` | 全局统一武器卡片组件；渲染预览网格、名称、星级、类型、属性、特殊效果和锁定状态 |
| `project/backpackShop.js` | 商店外壳、粒子、购买按钮；武器卡片内容调用共享渲染组件 |
| `project/backpack.css` | 商店与图鉴共用的 `.weapon-card-*` 视觉规范 |
| `main.js` | 把 `weaponCardRenderer`、`weaponCompendium` 加入纯数据脚本加载表 |
| `project/plugins.js` | 先安装武器系统和卡片组件，再安装背包、战斗、商店、合成与图鉴 |
| `project/floors/MT50.js` | 魔王坐标 `(6,5)` 的战后事件中提交当前背包武器的通关标记 |
| `tests/weaponCompendium.test.js` | 局外档案、出售保留、奖励、搜索筛选和接线回归测试 |

标题菜单按钮由图鉴文件运行时创建，不修改背包或战斗界面。事件也可以直接调用：

```javascript
core.plugin.weaponCompendium.open();
// 或兼容快捷入口
core.plugin.openWeaponCompendium();
```

## 3. 局外存储 API 结论

样板在 `libs/utils.js` 中提供以下同步 API：

```javascript
core.setLocalStorage(key, value);
core.getLocalStorage(key, defaultValue);
core.removeLocalStorage(key);
```

它们使用 `localStorage`，并自动给 key 添加 `core.firstData.name + "_"` 前缀。本项目名称为 `51_gbf`，所以图鉴的实际浏览器 key 是：

```text
51_gbf_weaponCompendiumProfileV1
```

该数据不在 `core.status.hero`、单局 flags 或普通存档数据中，因此新开游戏、读其他存档和重开不会清除图鉴解锁。

上一版曾误用 `weaponCodex*` 命名。当前实现只使用 `Weapon Compendium / weaponCompendium`；读取时会把旧局外键和旧单局 flag 合并迁移到新键，避免已经产生的解锁记录丢失。

样板也有异步的 `LocalForage` API，适合大存档或大对象。图鉴档案只保存 ID 和少量数值，体积很小，采用同步 LocalStorage 能保证获得武器和 50 层战后事件都能立即写入局外进度。

## 4. 数据模型

### 4.1 局外档案

```javascript
{
  version: 2,
  unlockedWeaponIds: ["I372", "I384"],
  clearedWeaponIds: ["I372"],
  claimedRewardIds: ["unlock_10"],
  values: {
    compendiumPoints: 1
  },
  stats: {
    victories: 1,
    unlockEvents: 1,
    lastUnlockedAt: "2026-08-23T00:00:00.000Z"
  }
}
```

- `unlockedWeaponIds` 使用 `project/weapons.js` 的顶层 key，而不是显示名称；改名不影响解锁。
- `clearedWeaponIds` 保存至少一次在通关瞬间仍位于背包中的武器；该数组中的武器会自动视为已解锁。
- `claimedRewardIds` 保证同一奖励永久只领取一次。
- `values` 保存配置化奖励产生的局外数值，不自动写入勇士、背包或战斗状态。
- `stats.victories` 记录图鉴系统成功提交的 50 层胜利次数。

首次读取不到档案时，`unlockedWeaponIds` 默认为空，即所有武器均未解锁。

### 4.2 本局历史

本局数据保存到单局 flag：

```javascript
flag:__weapon_compendium_run_obtained__ = ["I372", "I384"]
flag:__weapon_compendium_run_committed__ = true | false
```

- `__weapon_compendium_run_obtained__` 会随普通存档保存和读档恢复。
- 获得武器时只追加 ID，不提供删除路径，所以出售、合成消耗或其他背包删除不会丢失历史。
- `__weapon_compendium_run_committed__` 防止同一胜利存档重复提交、重复增加胜利次数或重复发奖。
- 新游戏重置 hero flags 后会自然得到一份新的本局历史。

## 5. 获得、出售与通关流程

```text
地图拾取 / 商店购买 / 免费奖励 / 合成产物
                    │
                    ▼
        背包公开 add API 成功返回
                    │
                    ▼
  追加本局历史 + 立即写入局外 unlockedWeaponIds
                    │
       ┌────────────┴────────────┐
       ▼                         ▼
  继续持有                    出售/合成消耗
       │                         │
       └────────────┬────────────┘
                    ▼
          击败 MT50 (6,5) 魔王
                    │
                    ▼
       只扫描通关瞬间仍在背包中的武器
                    │
                    ▼
   写入 clearedWeaponIds → 执行胜利事件
```

图鉴安装器装饰以下公开方法，并保持原参数、返回值和状态逻辑不变：

- `core.plugin.addBackpackWeapon`：商店、免费奖励、合成产物；
- `core.plugin.addBackpackItem`：地图拾取；
- `core.plugin.syncBackpackItems`：旧存档同步后补扫当前背包并立即补录解锁。
- `core.plugin.removeBackpackWeapon`：删除前保底补录该实例，覆盖旧存档武器首次操作就是出售或合成消耗的情况。

每次成功获得武器都会同步记录本局历史并永久解锁，出售或合成消耗不会回锁。背包 v6 存档只保存 `definitionId`、实例 ID、位置和旋转等基础信息；武器属性与规则始终从 `project/weapons.js` 的中央定义重建。通关提交只扫描 `flag:__backpack_state__` 中当时仍持有的 `definitionId`，已经卖出或被合成消耗的武器不会获得本次通关标记。

录像回放不会写局外档案，避免仅观看录像就改变玩家永久进度；本局 flag 仍可按录像过程正常计算。

## 6. 统一卡片与未解锁展示规则

商店、盲盒和图鉴都调用同一个组件：

```javascript
core.plugin.weaponCardRenderer.createCard(weaponDefinition, {
  lock: false,
  showCraftHammer: true,
  mobileListMode: true,
  actionButton: {
    label: "60",
    price: 60,
    onClick: buyWeapon
  }
});

core.plugin.weaponCardRenderer.createCard(weaponDefinition, {
  lock: true,
  showCraftHammer: false,
  mobileListMode: true,
  className: "weapon-compendium-entry"
});
```

共享组件固定负责以下区域，后续样式或字段调整只修改一处：

- 与背包坐标语义一致的武器预览网格、真实占格与联动格；
- 武器名称，以及同一信息行内左侧的武器类型标签和右侧的稀有度星级；
- 伤害、命中、攻击间隔、奥义获取四项属性；
- 特殊效果文本与联动箭头；
- `lock` 脱敏状态；
- 摘要信息行右侧的可配置操作按钮；
- 手机列表的详情展开与占格预览弹框。

商店仍单独负责稀有度粒子、价格计算与购买回调，盲盒负责免费获取回调；操作按钮本身由共享卡片组件渲染在类型和星级右侧。普通商店按钮只显示当前数字价格，免费盲盒按钮显示“获取”。图鉴仍单独负责搜索、筛选、解锁统计与奖励进度。

图鉴宽屏时通常与商店一致显示 5 列。分组网格使用 `auto-fill + minmax` 根据容器实际宽度动态减少列数，并强制 `overflow-x: hidden`；窄屏会依次变为 4、3、2、1 列，不产生横向滚动条。

永久档案中的 `clearedWeaponIds` 会让对应图鉴卡片增加 `.has-cleared-run`，整张卡片显示青绿色外发光 `box-shadow`，表示玩家曾使用该武器通关。该效果只出现在图鉴，不改变商店卡片和武器数据。

图鉴顶部总览同时显示 `已收集 X / 总数` 与 `已通关 Y / 总数`。已通关数量取 `clearedWeaponIds` 的唯一武器数，同一武器无论通关多少次都只统计一次。

未解锁卡片仍使用真实武器素材生成轮廓，但图片应用：

```css
filter: brightness(0);
```

以下内容统一脱敏：

- 名称：`???`
- 稀有度：`???`
- 类型：`???`
- 伤害、命中、攻击间隔、奥义：`???`
- 特殊描述：`???`
- 占格数量、尺寸和联动提示：`???`

`lock: true` 时不生成联动格，避免从联动范围反推出武器能力；卡片 DOM 同时带有 `.is-locked` 和 `data-locked="true"`，统一锁定样式和自动化测试入口。

组件也接受武器展示数据本身携带 `lock: true`；两种写法等价。锁定卡片会额外写入布尔 `lock` DOM 属性，便于其他界面或主题样式识别。

### 6.1 手机端交互

宽度不超过 `700px` 时，图鉴、商店和盲盒的共享武器卡片统一切换为单列紧凑列表。左侧显示 `76px × 76px` 的可点击武器缩略图；缩略图通过 `includeSynergy: false` 与 `paddingCells: 0` 只按武器自身占格取景，避免联动范围和外围棋盘把武器本体压小。右侧同一行显示武器名称、星级、类型、可选操作按钮和 SVG 展开箭头，不使用文本字符模拟图标。点击右侧非按钮区域展开或收起伤害、命中、攻击间隔、奥义获取与特殊效果，点击左侧缩略图则打开统一的占格预览弹框。未解锁武器在列表和预览弹框中都保持黑色图片，不会因打开弹框泄露原始武器素材。

占格预览继续调用共享卡片组件的 `buildWeaponPreview()`，不会复制占格或图片裁剪算法。已解锁武器的联动格在弹框中通过 `.is-synergy-visible` 始终显示，不依赖鼠标悬停；未解锁武器仍不生成联动格，避免泄露能力。弹框支持点击遮罩、关闭按钮和 `Esc` 关闭，关闭后焦点返回原武器缩略图。

公开的 `getEntries()` API 也返回脱敏结果，调用方无法从该 API 直接取得锁定武器的名称、属性、类型或特殊描述。

## 7. 分组、排序、搜索与筛选

### 7.1 分组

图鉴不是将全部武器直接平铺，而是由玩家选择以下分组方式：

- 按类型分组：类型名称按中文排序；拥有多个 `weaponTypes` 的武器会出现在它所属的每个类型组中；无类型武器进入“未分类”。
- 按稀有度分组：分组顺序固定为 5 星到 1 星，无法识别的数值进入“未知稀有度”。

每个分组标题右侧显示 `已收集 X / Y　已通关 Z / Y`，与顶部总览的统计文案保持一致。`Y` 是该分组的完整武器总数，`X` 是其中已经永久解锁的数量，`Z` 是其中带有永久通关标记的唯一武器数。名称搜索和收集状态筛选只缩小标题下方当前显示的卡片，不改变该分组的完整收藏与通关进度；类型筛选会同时缩小展示范围和进度统计范围。

每个分组内部仍使用商店同款五列武器卡片网格。

### 7.2 排序

“武器排序”作用于每个分组内部，支持：

- 默认顺序；
- 已收集优先、未收集优先；
- 名称升序、名称降序；
- 稀有度降序、稀有度升序。

锁定武器的显示名称仍是 `???`，名称排序不会把真实名称写入 DOM。

### 7.3 搜索与筛选

- 名称搜索忽略大小写和首尾空格。
- 真实名称仅对已解锁武器可搜索，防止搜索结果反向泄露锁定名称。
- 输入 `???` 可以搜索锁定条目。
- 类型下拉框来自所有中央武器定义的 `weaponTypes` 去重结果。
- 类型筛选可以显示该类型中的已解锁卡片和黑色锁定剪影，但锁定卡片仍不显示真实类型。
- 收集状态下拉框支持“全部武器、只看已解锁、只看未解锁、只看已通关、只看未通关”。
- “只看未通关”表示没有通关标记的全部武器，包括尚未解锁武器以及已经解锁但尚未使用其通关的武器。
- 名称搜索、类型筛选和收集状态筛选可以组合使用。

## 8. 配置化奖励

奖励只在 `project/weaponCompendium.js` 顶部配置对象的 `rewards` 数组中维护：

```javascript
var weaponCompendiumConfig_... = {
  storageKey: "weaponCompendiumProfileV1",
  runObtainedFlag: "__weapon_compendium_run_obtained__",
  runCommittedFlag: "__weapon_compendium_run_committed__",
  eventId: "weaponCompendium",
  rewards: [
    {
      id: "unlock_10",
      title: "小有收藏",
      condition: {
        type: "unlockedCount",
        operator: ">=",
        value: 10
      },
      rewards: [
        {
          type: "metaValue",
          key: "compendiumPoints",
          operator: "add",
          value: 1,
          text: "图鉴点数 +1"
        }
      ]
    }
  ]
};
```

当前正式 `rewards` 数组为空，避免在策划尚未确定具体奖励时改变游戏数值。填入配置后无需修改判定或 UI 代码。

### 8.1 支持的条件

| `condition.type` | 字段 | 含义 |
| --- | --- | --- |
| `unlockedCount` | `operator`, `value` | 永久解锁总数 |
| `unlockedRatio` | `operator`, `value` | 解锁比例，范围通常为 0～1 |
| `weaponUnlocked` | `weaponId` | 指定中央定义 key 是否已解锁 |
| `weaponTypeCount` | `weaponType`, `operator`, `value` | 指定类型的已解锁数量 |
| `allWeaponsUnlocked` | 无 | 是否已经全收集 |
| `metaValue` | `key`, `operator`, `value` | 局外 `profile.values[key]` 是否达标 |
| `all` | `conditions` | 所有子条件成立 |
| `any` | `conditions` | 任意子条件成立 |
| `not` | `condition` | 子条件不成立 |

数值比较支持 `>`, `>=`, `<`, `<=`, `=`, `==`, `===`, `!=`, `!==`，缺省为 `>=`。

组合条件示例：

```javascript
condition: {
  type: "all",
  conditions: [
    { type: "unlockedCount", operator: ">=", value: 30 },
    { type: "weaponTypeCount", weaponType: "剑", operator: ">=", value: 5 }
  ]
}
```

### 8.2 支持的奖励动作

| `reward.type` | 字段 | 含义 |
| --- | --- | --- |
| `metaValue` | `key`, `operator`, `value` | 修改局外数值；`operator` 支持 `add`（默认）、`set`、`max` |
| `unlockWeapons` | `weaponIds` | 解锁指定武器 key 数组 |
| `unlockWeaponType` | `weaponType` | 解锁指定类型的全部武器 |

奖励动作使用白名单解释器，不会执行配置中的任意字符串代码。包含未知动作类型的整项奖励会被跳过并输出警告，不会留下“已领取但未正确发放”的记录。

奖励可以继续解锁武器并触发后续奖励。系统采用有限轮次的连锁结算，上限为“奖励配置数 + 1”，同时依靠唯一 `id` 和 `claimedRewardIds` 防止循环与重复领取。

## 9. 公开 API

统一命名空间为 `core.plugin.weaponCompendium`：

```javascript
// 打开、关闭与状态
core.plugin.weaponCompendium.open();
core.plugin.weaponCompendium.close();
core.plugin.weaponCompendium.isOpen();

// 只读数据（返回安全副本）
core.plugin.weaponCompendium.getProfile();
core.plugin.weaponCompendium.getEntries({
  search: "剑",
  weaponType: "剑",
  collectionStatus: "unlocked" // "" | unlocked | locked | cleared | uncleared
});
core.plugin.weaponCompendium.getGroupedEntries({
  groupMode: "type",       // type | rarity
  sortMode: "rarityDesc", // default | collectedFirst | uncollectedFirst | nameAsc | nameDesc | rarityDesc | rarityAsc
  search: "剑",
  weaponType: "剑",
  collectionStatus: "uncleared"
});
core.plugin.weaponCompendium.getWeaponTypes();
core.plugin.weaponCompendium.getRunObtainedWeaponIds();
core.plugin.weaponCompendium.getConfig();

// 记录、主动解锁与通关提交
core.plugin.weaponCompendium.recordObtained("I372");
core.plugin.weaponCompendium.recordCurrentBackpack();
core.plugin.weaponCompendium.unlockWeapons(["I372"]);
core.plugin.weaponCompendium.completeRun();

// 策划、调试或其他局外系统调用
core.plugin.weaponCompendium.unlockWeapons(["I372", "I384"]);
core.plugin.weaponCompendium.evaluateCondition({ type: "unlockedCount", value: 10 });
core.plugin.weaponCompendium.evaluateRewards();
```

兼容快捷方法：

```javascript
core.plugin.openWeaponCompendium();
core.plugin.closeWeaponCompendium();
core.plugin.completeWeaponCompendiumRun();
core.plugin.unlockRunWeaponCompendium();
```

`completeRun()` 返回报告对象，其中 `clearedWeaponIds` 是本次新增通关标记，`unlockedWeaponIds` 仅包含旧存档兜底扫描时补录的解锁。`unlockRunWeapons()` 保留为 `completeRun()` 的兼容别名。

## 10. 隔离原则

图鉴不会：

- 修改 `project/weapons.js` 的中央定义；
- 修改武器实例、摆放格、背包容量、金币或出售规则；
- 修改攻击、联动、状态效果或战斗流程；
- 把永久解锁混进单局背包状态；
- 因武器出售或合成消耗而回锁图鉴；
- 在录像回放时写入永久进度。

图鉴和其他系统只通过三个稳定边界通信：中央武器定义、背包公开“添加” API、MT50 战后事件。

## 11. 验证

运行全部 Node 回归测试：

```powershell
node --test tests/*.test.js
```

图鉴专项测试覆盖：

1. 默认全部锁定及信息脱敏；
2. 游戏过程中成功获得武器立即写入永久解锁；
3. 卖出不回锁，且已卖出的武器不会获得本次通关标记；
4. 只给通关瞬间仍在背包中的武器添加通关标记；
5. 地图拾取记录和即时解锁；
6. 奖励条件、奖励动作与一次性领取；
7. 名称搜索、类型筛选、解锁/通关状态筛选、分组与排序；
8. 共享卡片 `lock` 属性、黑色剪影和通关 `box-shadow`；
9. 自适应列数与禁止横向滚动；
10. 主加载表、插件安装和 MT50 战后事件接线。

# 背包乱斗战斗系统设计文档

- 文档状态：已实现并接入项目（规则与实现同步稿）
- 更新时间：2026-08-18
- 适用项目：50层魔塔-背包乱斗
- 时间基准：`100 Tick = 1 回合`

## 1. 目标

新增一套独立的背包乱斗战斗模式。战斗时不再把背包内所有武器的攻击简单相加，而是让每个已摆放武器拥有独立冷却时间；冷却完成后，该武器独立进行命中、伤害、特殊效果和奥义获取结算。

本系统需要满足以下目标：

1. 每把武器独立走 CD，攻击间隔越短，单位回合内攻击次数越多。
2. 防御属性完全舍弃：玩家和怪物都不使用防御减伤，伤害直接按税前全额结算（格挡仍生效）。
3. 支持可扩展、数据驱动的 Buff、Debuff 和武器触发效果。
4. 所有实际战斗随机行为使用同一个、由开局种子固定的随机数生成器。
5. 支持录像在相同种子下得到完全相同的战斗和抽奖结果。
6. 提供不推进实际随机状态的种子预演伤害模拟器，并缓存结果供地图显伤和怪物手册使用。
7. 移除原有魔塔临界值计算和临界显示。
8. 战斗模拟与渲染分离；倍速、掉帧和录像快进不能改变战斗结果。

## 2. 现有基础与改造边界

当前武器中央定义表 `project/weapons.js` 已包含以下基础字段：

- `minAttack`：伤害下限。
- `maxAttack`：伤害上限。
- `hitRate`：基础命中率，范围为 0～1。
- `attackInterval`：攻击间隔，单位为回合/战斗秒。
- `ultimateGain`：武器每次普通攻击获得的基础奥义值。
- `synergyRules`：布局及静态属性联动。

当前背包插件已经可以提供已摆放武器实例、位置、旋转和联动后的属性。新战斗系统应复用这些接口，不建立第二份武器属性表。

现有战斗入口是同步执行的，现有 `afterBattle` 又同时包含经典伤害计算、奖励、怪物移除和战后事件。实现时需要将其拆分为：

1. 战斗启动与控制。
2. 战斗过程模拟。
3. 胜负结果写回。
4. 通用胜利奖励和地图清理。

不修改 `libs/` 内核心引擎文件；通过 `project/` 数据、函数和插件完成覆写与接入。

## 3. 核心时间模型

### 3.1 Tick 与回合

- `100 Tick = 1 回合`。
- 1 倍速下，1 回合对应现实时间 1 秒。
- 2 倍速、4 倍速只缩短现实播放时间，不改变 Tick 数、事件顺序或随机数调用顺序。
- 战斗逻辑只使用整数 Tick，避免浮点时间累计误差。
- 武器间隔换算方式：`intervalTicks = round(attackInterval × 100)`。
- 最终攻击间隔最低为 1 Tick。

例如攻击间隔为 0.25 的武器，间隔为 25 Tick，会在第 25、50、75、100 Tick 分别触发一次攻击，即每回合攻击 4 次。

### 3.2 初始 CD

- 玩家武器和怪物的初始 CD 都从 0 开始。
- 必须经过一个完整攻击间隔才会第一次攻击。
- 怪物默认攻击间隔为 1 回合，即 100 Tick。
- `attackInterval` 为空的物品不主动攻击，只能作为被动、资源或事件触发型物品。

### 3.3 动态攻击间隔

每个攻击单位保存当前已累计 CD 和当前有效攻击间隔。冰洁、激奏等效果改变攻击间隔后，当前 CD 立即按照新阈值判断：

- 若新间隔已经小于或等于当前累计 CD，则在当前 Tick 进入待攻击队列。
- 攻击完成后，该武器自身 CD 清零。
- 奥义产生的额外攻击不读取、不清零、不推进武器原 CD。

### 3.4 同 Tick 事件顺序

同一个 Tick 内固定按以下顺序结算：

1. 检查玩家或怪物是否已经死亡。
2. 若为整回合边界，结算该边界之前已经存在的固定回合效果。
3. 结算玩家就绪武器，按背包实例稳定顺序执行。
4. 每次普通攻击后立即处理可能触发的奥义队列。
5. 结算怪物就绪攻击。
6. 再次检查胜负并刷新只读渲染快照。

在第 100 Tick 才新增的烧伤或再生，不参与第 100 Tick 开头的回合结算，第一次结算发生在第 200 Tick。

## 4. 战斗状态结构

每场战斗创建独立的临时状态，战斗结束后销毁：

```text
battleState
├─ version
├─ tick
├─ round
├─ speed
├─ rngCallCount
├─ player
│  ├─ hp / maxHp / atk
│  ├─ ultimate
│  ├─ buffs
│  └─ debuffs
├─ enemy
│  ├─ id / hp / maxHp / atk
│  ├─ attackIntervalTicks / cooldownTicks
│  ├─ buffs
│  ├─ debuffs
│  ├─ combatRules
│  ├─ runtimeCounters
│  └─ expectationAccumulator
├─ weapons[]
│  ├─ instanceId
│  ├─ sourceItemId
│  ├─ cooldownTicks
│  ├─ calculatedAttributes
│  └─ runtimeCounters
├─ eventQueue
└─ battleLog
```

Buff、Debuff、CD、奥义和武器战斗计数默认只在当前战斗中存在；勇士 HP、奖励、统计数据和地图状态在战斗结束时写回游戏。

## 5. 防御与伤害

### 5.1 防御已舍弃

防御属性不再参与战斗。双方受到的伤害直接等于格挡抵扣后的剩余税前伤害：

```text
最终伤害 = max(0, 税前伤害 - 格挡抵扣)
```

不再使用 `攻击 - 防御` 的旧减法公式，也不使用 `def/(def+600)` 比例减伤。勇士和怪物身上的 `def` 字段仅为数据兼容保留，不参与任何伤害结算。

### 5.2 武器普通攻击

一次武器普通攻击按以下流程执行：

1. 武器 CD 完成并发动攻击。
2. 从统一随机数生成器获取一次命中判定。
3. 未命中：不造成伤害，不触发任何攻击附带效果；仍按“完成一次普通攻击”获取奥义，然后重置 CD 并结束本次攻击。
4. 命中：在 `minAttack`～`maxAttack` 中随机取得本次税前伤害。
5. 防守方格挡抵扣税前伤害。
6. 剩余税前伤害直接扣除 HP。
7. 触发该武器全部命中后效果。
8. 结算本次普通攻击的奥义获取；命中与未命中使用同一套奥义获取规则。

命中率：

```text
实际命中率 = clamp(基础命中率 - 黑暗层数 × 0.05, 0, 1)
```

### 5.3 伤害精度

- 实际战斗内部使用固定精度数值，建议保留到千分位。
- 不在每次攻击后强制把小数伤害向下取整，避免高攻速、低单次伤害武器因逐次取整完全失效。
- UI 显示时可以取整，但内部 HP 和累计伤害以固定精度值为准。
- 烧伤属于直接伤害，不受格挡影响。

## 6. 奥义系统

玩家拥有一个全队共享奥义槽，范围至少支持 0～100 以上的临时溢出值。

普通攻击的奥义获取量：

```text
实际奥义获取 = max(0, 武器基础奥义获取 + 高扬层数 × 2 - 虚脱层数 × 2)
```

规则：

1. 普通攻击发动后按武器 `ultimateGain` 获取奥义。
2. 武器未命中时没有伤害和命中后效果；奥义获取仍按“该武器完成一次普通攻击”结算。
3. 奥义达到或超过 100 时，立即扣除 100 并触发一次奥义。
4. 奥义使背包内所有具备主动攻击能力的武器立即额外攻击一次。
5. 奥义攻击正常进行命中、随机伤害和命中后效果结算。
6. 奥义攻击不影响任何武器原 CD。
7. 奥义攻击不获取奥义，避免递归无限触发。
8. 若普通攻击一次获得大量奥义，使奥义槽仍大于或等于 100，则可以继续触发下一次奥义。

## 7. Buff、Debuff 与即时效果

### 7.1 状态定义

| 状态 | 类型 | 实际效果 | 固定回合结算 |
|---|---|---|---|
| 冰洁 | Debuff | 每层使所属一方所有攻击间隔增加 1 Tick | 否 |
| 烧伤 | Debuff | 每回合造成 `当前层数 × 10` 点直接伤害 | 100 Tick |
| 黑暗 | Debuff | 每层使所属一方命中率降低 5% | 否 |
| 虚脱 | Debuff | 每层使奥义获取量降低 2，最低降至 0 | 否 |
| 反射 | Buff | 即将获得 Debuff 时消耗 1 层，将其中 1 层反弹给施加者 | 否 |
| 格挡 | Buff | 每层抵扣下次受到的 1 点税前伤害，未消耗层数保留 | 否 |
| 刻印 | Buff | 作为其他武器消费或检测的资源，本身没有直接效果 | 否 |
| 高扬 | Buff | 每层使奥义获取量增加 2 | 否 |
| 激奏 | Buff | 每层使自身全部武器攻击间隔减少 1 Tick，最低 1 Tick | 否 |
| 再生 | Buff | 每回合先恢复 `结算前层数 × 10` HP，再减少 1 层 | 100 Tick |

### 7.2 即时效果

净化和驱散属于即时动作，不作为持续状态保存：

- 净化：攻击命中后，使自身所有当前 Debuff 分别减少 1 层。
- 驱散：攻击命中后，从敌方所有大于 0 层的 Buff 中随机选择一种并减少 1 层。

玩家武器和敌人都可以执行净化与驱散。敌人通过 `combatRules` 或简化字段（`cleanse`、`dispel`）配置后，在其攻击命中玩家时触发对应效果。敌人净化时清除自身 Debuff，驱散时从玩家 Buff 中随机选择一种减少 1 层。

即时效果没有持续图标，但需要播放图标闪现、浮动文字或战斗日志提示。

### 7.3 命中要求

所有武器附带的攻击效果都必须在本次攻击命中后触发。Miss 时：

- 不造成伤害。
- 不附加 Buff 或 Debuff。
- 不执行净化、驱散等命中后动作。
- 不触发“命中时”“攻击命中若干次”等联动计数。
- 武器 CD 正常重置。

### 7.4 反射处理

每一层待附加 Debuff 单独处理：

1. 检查目标是否有反射。
2. 若有，消耗目标 1 层反射。
3. 本层 Debuff 不附加给原目标，改为附加给施加者。
4. 被反射的 Debuff 标记为反射来源，不再触发第二次反射，防止无限循环。

### 7.5 狼皮处理

狼皮（`wolfSkin`）是**不可叠加**的 buff（每次施加最多 1 层，层数仅控制持续时间），在 `applyStatus` 的 Debuff 路径中：

1. 反射检查之后、正式附加之前。
2. 若目标方（受击方）有 wolfSkin（层数 ≥ 1）：本层 Debuff 直接消耗（不反弹、入栈），追加"狼皮吸收"日志。
3. 与反射互斥：优先反射回弹，再检查狼皮吸收。
4. 狼皮存在时若叠加剑/斧的 `statusDamageBonus`/`statusExtraAttack` 注册，叠加 +5 伤害 / +1 攻击次数生效（与层数无关，仅要求狼皮存在）。
5. 狼皮 `periodic: true, periodTicks: 100`：`settlePeriodicStatuses` 每秒减 1 层直至 0 消失。

### 7.6 专属 buff（exclusive）

刻印（`mark`）/MP（`mp`）/狼皮（`wolfSkin`）/黑之魅力（`blackCharm`）在状态注册表中标记 `exclusive: true`：

1. **不可被驱散**：`dispelBuff`（随机驱散）、`dispelBuffPercent`（百分比驱散）、`dispelLastBuff` 均跳过专属 buff。
2. **不参与随机 buff 池**：`getAllBuffIds` 排除 `exclusive: true` 的 buff，`applyRandomBuffs` 默认池不含专属 buff（可用 `pool` 显式指定强行随机）。
3. 其余 buff（反射/格挡/高扬/激奏/再生）可正常驱散、可被随机。

### 7.7 黑之魅力处理

黑之魅力是 kind buff、periodic periodTicks=100、stackable true、exclusive true。每秒 `settlePeriodicStatuses` 结算：

1. 对玩家/敌方侧自身施加 `reflection` / `regeneration` / `block` 各 1 层。
2. 检测 MP 层数：MP ≥ 4 → 消耗 4 层；MP < 4 → 解除黑之魅力（仍施加上述 3 个 buff，但不消耗 MP）。
3. 与狼皮不同的是：黑之魅力可叠加（多层 buff），每层独立结算。

### 7.8 MP 消耗累计与奥义伤害

`consumeMp(state, sideKey, amount)` 消耗 MP（专属指示物）并累加 `state.mpConsumedTotal`（黑之魅力每秒 4 层消耗等均计入）。效果 `dealMpConsumedDamage`（配合 `afterUltimate`）造成 `mpConsumedTotal × multiplier` 的直接伤害（无视格挡）。

### 7.9 无敌（setInvincible）

效果 `setInvincible` 设置 `state.player.invincibleUntilTick = tick + durationTicks`（默认 500 = 5 秒）；`applyDamage` 对玩家在无敌期内直接返回伤害 0（格挡也不消耗）。配合规则级 `once: true` 实现"每场战斗只能触发 1 次"。

### 7.10 奥义多段攻击（repeatAttack）

效果 `repeatAttack` 触发**该武器**立即再攻击 `count` 次（origin=ultimate、`ultimateMode: costOnly` 不消耗奥义值）。配合 `afterUltimate` 触发器与 `chance` 概率条件（`base + 附近匹配武器数 × nearbyBonus`）实现"奥义发动时 30% 概率使该武器的攻击发动 9 次，每有 1 把上下左右一格内的刀概率 +10%"。

### 7.11 同名武器机制（sameNameCount / sameNameDamageBonus）

条件 `sameNameCount` 统计与当前武器 `name` 相同的武器数（含自身）并比较（如 `value: 3` 表示"配置 3 个及以上同名武器"）。效果 `sameNameDamageBonus` 按 `id` 幂等注册：满足"同名 ≥ threshold（默认 3）"时该武器伤害 +`value`（二进制，与数量无关）；`weaponTypes` 可限定适用类型。核心与预计内核的攻击伤害计算均已叠加该加成。

### 7.12 附近动物双机制（nearbyRandomBuff / nearbyIntervalPercentBonus）

- `nearbyRandomBuff`：**任意触发器可用**（典型 `battleStart` 一次性结算，或 `afterHit` 每次命中结算）——每有 `every` 个附近匹配武器（directions/distance/filter），复用 `applyRandomBuff` 随机逻辑施加 1 个强化 buff（默认池为全部非专属增益，可用 `pool` 限定）。
- `nearbyIntervalPercentBonus`：间隔计算时实时结算——每有 `every` 个附近匹配武器，攻击间隔 ×(1-value)，**线性累加**（2 只 value 0.1 = -20%），在 `getWeaponIntervalTicks` 的加减法修正之后、取整之前应用。

### 7.13 扣血与残血恢复（damageSelf / healPercent / hpPercent / combatFlag）

- `damageSelf`：自身扣除 HP（可致死）；`healPercent`：按最大 HP 百分比恢复（`value` 为小数）。
- 条件 `hpPercent`：目标当前 HP 与 **state.battleMaxHp**（进战斗时的血条满值快照）之比与 value 比较（如"自身 HP 掉到 1/3 以下"）；战斗中 maxHp 变化不影响分母。
- 条件 `combatFlag`（`negate` 取反）+ 效果 `setCombatFlag`：规则开关——恢复效果（once）触发后设置标记，扣血规则用 `combatFlag + negate: true` 条件在标记未设置时才扣血，实现"该效果触发后本武器攻击不再扣除自身 HP"。

### 7.14 全局奥义获得加成（globalUltimateGainBonus）

效果 `globalUltimateGainBonus` 把 `state.globalUltimateGainBonus` 累加 `value`；`getUltimateGain(side, baseGain, combatState)` 计算时叠加该加成——**所有武器**每次攻击的奥义获得量 +value（多规则累加）。调用处（core/estimate 的 canPayWeaponUltimate 与攻击奥义结算）均传 combatState。

### 7.15 强化效果获得计数（buffGainCounter）

效果 `buffGainCounter` 注册到 `state.buffGainCounters`（id 幂等，`every`/`damage`）；`applyStatus` 对玩家施加 buff（kind==="buff"）后调用 `accumulateBuffGains`：累计获得层数，每满 `every` 层对敌方造成 `damage` 点直接伤害（剩余层数继续累计）。跨规则、跨来源（随机 buff/命中施加等）统一计数。

### 7.16 自身 HP 回复触发器（afterHeal）

`heal(state, sideKey, amount, handlers)` 在玩家回血 >0 时触发 `afterHeal` 规则（context 含 `healAmount`）。`state.afterHealActive` 防止规则内再次回血造成无限递归（一次回血至多触发一轮）；配合 `once: true` 实现"自身HP回复时仅触发一次"。触发来源覆盖再生、回血效果、饮料联动回血等一切玩家回血。core 与预计内核的 `settlePeriodicStatuses` 调用均传入各自 handlers（随机/联动类效果可用）。

### 7.5 格挡处理

```text
本次抵扣 = min(格挡层数, 本次税前伤害)
剩余格挡 = 格挡层数 - 本次抵扣
剩余税前伤害 = 本次税前伤害 - 本次抵扣
```

格挡抵扣后剩余税前伤害直接结算。若格挡多于本次伤害，没有消耗的层数保留到后续攻击。

## 8. 固定回合效果与 CD 展示

烧伤、再生以及未来配置为固定回合触发的状态，统一挂在全局回合时钟上：

- 触发点为第 100、200、300……Tick。
- 不是从获得状态的现实时间开始单独计时。
- 暂停时 Tick 不推进，状态 CD 也不推进。
- 倍速只改变现实播放速度，仍在相同 Tick 触发。

每个固定回合状态图标需要显示一个同步的 CD 遮罩或环形进度：

```text
roundProgress = battleTick % 100
remainingTicks = roundProgress == 0 ? 100 : 100 - roundProgress
progressRate = roundProgress / 100
```

所有固定回合状态共享同一个回合相位。图标提示中需要显示“距离下次触发还有 N Tick”或对应的小数回合。

## 9. Buff 图标与说明交互

### 9.1 图标注册表

所有持续 Buff 和 Debuff 必须在统一状态注册表中提供：

```text
id
名称
Buff/Debuff 类型
图标位置或图片名
是否叠层
是否固定回合触发
触发周期
动态效果说明格式
排序优先级
```

建议使用一张统一的 Buff 图标图集，避免大量独立图片请求。玩家和怪物状态区域共用同一套图标注册表。

### 9.2 显示内容

图标右下角显示当前层数；固定回合状态叠加显示 CD 环或遮罩。鼠标悬停、键盘聚焦或移动端长按时显示当前实际效果，而不是固定说明：

- 冰洁 12 层：`全部攻击间隔 +0.12 回合`。
- 烧伤 5 层：`下次回合结算造成 50 点直接伤害，剩余 37 Tick`。
- 黑暗 3 层：`命中率 -15%`。
- 虚脱 4 层：`每次奥义获取 -8`。
- 反射 2 层：`可反射接下来 2 层 Debuff`。
- 格挡 7 层：`可抵扣接下来共 7 点税前伤害`。
- 刻印 10 层：`当前资源：10 层`。
- 高扬 4 层：`每次奥义获取 +8`。
- 激奏 6 层：`全部武器攻击间隔 -0.06 回合`。
- 再生 3 层：`下次恢复 30 HP，随后变为 2 层，剩余 37 Tick`。

## 10. 武器动态效果配置

所有武器战斗属性和效果仍集中维护在 `project/weapons.js`。现有 `synergyRules` 继续处理布局和静态属性变化；新增 `combatRules` 处理战斗事件。

建议支持的触发器：

- `battleStart`
- `roundStart`
- `afterHit`
- `afterTakeDamage`
- `beforeReceiveDebuff`
- `afterAllyHit`
- `afterUltimate`
- `battleEnd`

建议支持的效果类型：

- `applyStatus`
- `applyRandomDebuff`（从减益池随机选取 1 个施加；实战与预计均按同一 `core.randBattle` 算法取值，预计只推进局部种子）
- `removeStatus`
- `cleanseAllDebuffs`
- `dispelRandomBuff`
- `dealDamage`
- `heal`
- `modifyWeaponStat`
- `consumeStatus`
- `gainUltimate`
- `triggerWeaponAttack`
- `statusDamageBonus`（状态驱动的全局武器伤害加成：按 id 幂等注册，攻击时按当前状态层数实时计算 `floor(层数/every) × value`，支持 `weaponTypes` 过滤与 `target: "enemy"/"opponent"`（读取敌方状态），`scope: "nearby"` 时仅作用于注册武器 directions/distance 范围内武器（缺省不含自身），不累积、天然可逆）
- `statusIntervalBonus`（状态驱动的攻击间隔修正：按 id 幂等注册，间隔计算时按目标阵营当前状态层数实时计算 `floor(层数/every) × value` 回合，支持 `weaponTypes` 过滤与 `target: "enemy"`，不累积、天然可逆）
- `statusExtraAttack`（状态驱动的额外攻击次数：按 id 幂等注册，攻击时按目标阵营当前状态层数实时计算 `floor(层数/every) × value`；每次攻击多次伤害、多次联动、奥义获取只判一次）
- `statusHitRateBonus`（状态驱动的命中率修正：目标方拥有状态（层数≥1）时本次攻击命中率 +value，存在即生效、与层数无关，应配合 beforeAttack，最终夹在 0~1）
- `goldMultiplier`（击败敌人获得的金币增幅：多条规则按 value 累加，`value: 1` 表示金币增加一倍即 ×2，两把叠加为 ×3；建议配合 `battleEnd` 使用；倍率经战斗结果带出，在经典结算处应用，与幸运金币道具叠加）
- `goldBonus`（战后金币固定增加 `value` 点，多条规则累加；建议配合 `battleEnd`，经战斗结果带出在经典结算处与倍率叠加）
- 条件 `buffStacks` + 效果 `consumeBuffs`（自身强化总层数检查 + 随机消耗层数；实战与预计均逐层随机选取）
- 效果 `addExtraAttack`（directions/distance/filter 范围内武器发动次数+1：每次攻击造成多次伤害、触发多次战斗联动，但奥义获取只判定一次，不随发动次数翻倍）
- 效果 `nearbyExtraAttack`（注册武器范围内每有 every 个匹配武器，其攻击次数 +value；支持 `relation: "sideBox"` + `span` 矩形区域，天然可逆）
- 效果 `nearbyIntervalBonus`（注册武器范围内每有 every 个匹配武器，其攻击间隔 +value 回合；支持 sideBox 矩形区域，天然可逆）
- 效果 `ignoreBlockAlways`（本武器常驻无视目标格挡抵扣，无需每次 beforeAttack 设置）
- 效果 `nearbyDamageBonus`（注册武器范围内每配置一个匹配武器，其攻击伤害 +value；支持 `relation: "sideBox"` + `span` 矩形区域，天然可逆）
- `triggerWeaponEffects`（立马发动附近匹配武器的全部效果：忽略目标武器的 trigger/conditions/once 限制，不重置其 CD，内部抑制链式递归）
- 规则级 `once: true`（整场战斗仅触发一次，按所属武器记录；被 triggerWeaponEffects 立即发动的规则不受此限制）
- `modifyWeaponStat` 支持 `weaponTarget: "nearby"`（本武器 + directions/distance/filter 匹配的附近武器，严格正交）；`attackInterval`（回合）修正会换算为 Tick 与 `attackIntervalTicks` 叠加生效
- 条件 `chance`（概率判定：`base` + 附近武器数×`nearbyBonus`，实战与预计均经 handlers.rollChance 按各自随机流掷骰）
- 效果 `guaranteeHit`（本次攻击必定命中，无视命中率与黑暗修正；应配合 `beforeAttack`，实战与预计同步生效）
- 效果 `ignoreBlock`（本次攻击无视目标格挡抵扣，格挡层数不被消耗；应配合 `beforeAttack`，实战与预计同步生效）
- 触发器 `beforeReceiveDamage`（怪物命中、伤害结算前，配合 `modifyReceivedDamage` 概率减伤）
- 效果 `modifyUltimate`（奥义值增减/百分比，`target: "enemy"` 修改敌方奥义）
- 触发器 `afterShieldEffect` + 条件 `nearbyShieldTriggered`（"附近盾牌的被攻击效果发动时"联动：被攻击类规则实际发动时按武器标记，供左右一格武器观察）
- 触发器 `afterAttack`（本武器攻击后，无论命中与否都触发；Miss 也生效）
- 效果 `applyRandomBuffs`（随机 count 次、每次从增益池随机选 1 个 buff 施加 stacks 层，可重复叠加；实战与预计使用相同的随机选取规则；默认池为全部 buff）

战斗代码不得通过武器 ID 编写专用 `switch`；新增武器效果应尽量只增加中央数据配置。

## 11. 统一随机数生成器

### 11.1 引擎随机数

当前引擎提供基于开局种子的 `core.rand()`，项目层另外提供两个相同调用格式的独立随机方法：

- 新游戏开始时生成并保存初始 `__seed__`。
- `core.randShop()` 专供商店抽取，当前状态保存在 `__randShop__`。
- `core.randBattle()` 专供怪物能力生成、实际战斗和战斗预估，当前状态保存在 `__randBattle__`。
- 两个独立流首次使用时都从 `__seed__` 初始化，随后只推进各自的 flag；存档会保存当前随机状态。
- 录像使用相同开局种子，并按相同调用顺序得到相同结果。

商店和战斗必须调用各自的随机方法，不得使用：

- `Math.random()`
- `core.rand2()`
- 商店或战斗逻辑中的通用 `core.rand()`

### 11.2 使用范围

随机行为按用途分流：

1. 武器命中、伤害上下限、净化、驱散、随机 Buff/Debuff 和怪物战斗能力使用 `core.randBattle()`。
2. 武器商店刷新、购买候选和免费赠予候选使用 `core.randShop()`。
3. 后续新增随机行为必须明确归入商店流、战斗流或通用流，不能跨用途推进随机状态。

随机整数伤害建议采用闭区间：

```text
damage = minAttack + core.randBattle(maxAttack - minAttack + 1)
```

命中判定：

```text
isHit = core.randBattle() < effectiveHitRate
```

### 11.3 确定性约束

- 随机数只能在确定性的战斗事件中获取。
- UI 渲染、粒子、音效、CD 动画不得消耗游戏随机数。
- 1 倍、4 倍、掉帧补帧和录像快进必须产生完全相同的随机调用顺序。
- 预计任务由主线程只读获取当前 `__randBattle__`，把数值副本随战斗快照交给 Worker。Worker 必须使用与 `core.randBattle()` 完全相同的推进公式，但只能修改任务局部种子，禁止调用真实随机方法或回写任何 flag。查看怪物手册或地图显伤前后，实际战斗和商店使用的随机状态必须逐位不变。
- 调试日志需要记录战斗内随机调用序号，便于定位回放差异。

## 12. 快速预计战斗伤害

### 12.1 输出语义

魔塔地图中的“战斗伤害”指勇士预计受到的总伤害。模拟器输出：

```text
damage：预计受伤
rounds：预计战斗回合数
canWin：按预计受伤判断能否获胜
roundsExceeded：预计所需回合数是否大于 10000
```

每只怪物只产生一个由当前种子决定的确定性预演结果：

- 武器每次攻击都按 `core.randBattle` 同算法在伤害上下限中真实取值。
- 命中、概率条件、净化、驱散、随机 Buff/Debuff 和随机消耗均真实掷骰。
- 每个怪物只运行一次预计模拟，不再分别计算最好和最坏结果。

预计模拟不以勇士死亡作为终止条件。即使累计受伤已经大于或等于勇士当前 HP，也继续以相同战斗状态推进，直到怪物死亡，并返回完整的预计最终受伤；该数值不截断为勇士 HP。`canWin` 在模拟完成后，再用当前勇士 HP 与预计最终受伤比较得出。因此“打不过”仍然显示具体受伤数值，只改变数值颜色和能否获胜标记。

### 12.2 当前随机种子的真实预演

预计输入包含读取任务时的当前 `__randBattle__` 数值。该 flag 尚未创建时使用开局 `__seed__`。Worker 以该值创建局部随机流，并复刻 `libs/utils.js` 的 `__next_rand`/`core.randBattle` 算法。每个怪物任务都有独立副本，因此地图上所有怪物都表示“如果下一场立即与该怪物战斗”的真实结果；任务执行不会推进实际战斗种子。

随机调用顺序与实际战斗一致：

```text
命中掷骰 -> 命中后逐次伤害取值 -> 命中效果随机数 -> 攻击后概率/随机效果 -> 怪物命中及其随机效果
```

必中效果与实际战斗一样跳过命中掷骰；固定上下限仍会和实际战斗一样消费一次伤害随机数。净化、驱散、随机 Buff/Debuff 与随机消耗都从当时的有效候选数组中真实选取，不再使用固定首项、末项或轮流取值。

### 12.3 迭代上限

- 单次预计模拟允许完整计算到第 10000 回合结束；10000 回合等于 1,000,000 Tick。
- 实现时不必逐 Tick 空转，可以直接跳到下一个武器就绪、怪物攻击或整回合状态结算点。
- 勇士预计 HP 降到 0 或以下时不得停止模拟，仍需继续累计怪物造成的伤害，直到怪物死亡。
- 怪物在第 10000 回合内死亡时必须返回具体数值，即使勇士打不过，也不能显示 `???`。
- 只有能够确定模拟需要进入第 10001 回合，即所需回合数严格大于 10000 时，才设置 `roundsExceeded`，地图显伤显示 `???`。
- 模拟器必须有无进展检测。如果状态循环能够证明怪物在 10000 回合内不可能死亡，可以直接设置对应的回合超限标记，无需继续空转；这个提前退出仍然属于“所需回合数大于 10000”，不能用于其他错误场景。
- Worker 创建失败、运行异常或输入数据错误均不得显示 `???`，应显示“计算失败”，以保证 `???` 只代表回合数大于 10000。

### 12.4 缓存设计

预计伤害结果和正在执行的计算任务都必须缓存。缓存使用内存 `Map` 或 LRU，不写入存档，避免版本升级后读取陈旧结果。

缓存键至少包含：

```text
battleRuleVersion
weaponConfigVersion
backpackLayoutRevision
placedWeaponsHash
heroDefenseAndBattleStatsHash
enemyCombatSnapshotHash
difficultyAndFloorRatioHash
currentRandomSeed
```

说明：

- `backpackLayoutRevision` 在武器放置、收回、旋转、删除、添加或自动整理后递增。
- `placedWeaponsHash` 包含实例来源、位置、旋转以及战斗相关最终属性。
- 当前勇士 HP 和战斗最大 HP 均进入完整战斗输入及缓存键，确保回血、血量比例条件和自伤能按当前状态重新计算。
- 防御不再影响受伤，勇士防御不进入缓存键；其他会改变受伤的属性必须进入缓存键。
- 敌人使用最终战斗快照作为键，因此同 ID、同最终属性的怪物可以跨坐标复用；若楼层倍率、光环或定点属性不同，会生成不同键。
- 武器数据或战斗规则变化时通过版本号整体失效。
- 当前 `__randBattle__` 数值必须进入缓存键；随机调用次数和模拟结束种子只用于校验，不参与同一任务的初始键。

缓存失效条件：

1. 背包内武器排列或旋转发生变化。
2. 已摆放武器数量或定义发生变化。
3. 勇士其他会改变受伤的战斗属性变化。
4. 怪物 HP、攻击、攻击间隔或战斗特性变化。
5. 楼层倍率、难度或战斗公式版本变化。
6. 当前 `__randBattle__` 因实际战斗或怪物战斗能力生成而发生变化；`__randShop__` 变化不影响战斗预估缓存。

缓存项包含 `pending`、`ready`、`error` 三种状态。对同一缓存键，地图刷新、怪物手册翻页和重复查看只能创建一个计算任务：`pending` 时复用同一任务，`ready` 时直接返回安全副本；短暂的 `error` 状态用于避免每帧重复创建失败任务，并允许在 Worker 重建后重试。

### 12.5 Web Worker 异步计算

预计伤害模拟必须在 Web Worker 中执行，不得在地图绘制、怪物手册绘制或主线程动画帧中同步运行完整模拟。

主线程流程：

1. 生成可序列化的勇士、背包武器、怪物和规则快照，并计算缓存键。
2. 查询缓存；`ready` 立即使用结果，`pending` 显示“计算中”，未命中则写入 `pending` 并向 Worker 提交任务。
3. Worker 从输入种子建立局部随机流，完成单次真实战斗预演后将结果发回主线程。
4. 主线程校验 `requestId`、缓存键、背包排列版本、战斗规则版本和起始随机种子。过期结果直接丢弃，不能覆盖新排列或新种子的结果。
5. 校验通过后将缓存项改为 `ready`，只刷新相关地图显伤；怪物手册仍处于打开状态时同步刷新对应条目。

建议消息结构：

```text
主线程 -> Worker：requestId、cacheKey、layoutRevision、battleRuleVersion、randomSeed、inputSnapshot
Worker -> 主线程：requestId、cacheKey、layoutRevision、battleRuleVersion、randomSeed、result 或 error
```

实现约束：

- 复用一个常驻 Worker，或使用数量固定的小型 Worker 池；禁止为每只怪物临时创建一个 Worker。
- 同一缓存键只允许一个在途任务，所有调用方共享结果通知。
- 预计模拟核心必须与 DOM、Canvas 和全局 `core` 解耦。Worker 只接收数值种子，不持有或调用实际游戏随机服务。
- `inputSnapshot` 和 Worker 消息必须包含起始 `randomSeed`，不得包含可回写的 flag 引用或 RNG 对象。
- 预计模块和 Worker 禁止调用 `core.rand()`、`core.randBattle()`、`core.rand2()`、`gameRandom`、`Math.random()`、`crypto.getRandomValues()`；只允许局部实现 `core.randBattle` 的确定性推进公式。
- 相同战斗快照和相同起始种子必须得到完全相同的逐 Tick 状态、随机调用次数、结束种子和最终结果。
- 地图同屏出现大量怪物时，优先提交当前楼层可见条目；相同最终怪物快照继续按缓存键合并。
- 重新开始游戏、切换工程版本或销毁页面时终止旧 Worker 并清空在途任务；新 Worker 就绪后再接受计算。
- Worker 报错时将缓存项标记为 `error`，记录错误并显示“计算失败”；不得回退到会阻塞主线程的同步完整模拟。

## 13. 地图显伤和怪物手册

地图显伤改为显示预计受伤单值：

```text
缓存未命中或任务进行中：显示“计算中”
计算完成：显示预计受伤单值
预计受伤达到当前 HP：仍显示具体数值，并以红色标记为致死
所需回合数 > 10000：显示“???”
Worker 或输入异常：显示“计算失败”
```

怪物手册建议显示：

- 预计受伤单值。
- 预计战斗回合数。
- 怪物攻击间隔。
- 怪物攻击。
- 怪物 Buff/Debuff 能力（点击怪物后的详细信息页展示）。
- 是否存在未实现或导致估算截断的效果。

怪物详情页的 Buff/Debuff 能力展示规则：

- 读取敌人 `project/enemys.js` 中的简化字段（`burn`、`ice`、`darkness`、`exhaustion`）与自定义 `combatRules`，归纳为三类：
  - 减益施加（对勇士）：显示为橙色，例如 `烧伤×4`。
  - 增益获得（自身）：显示为绿色，例如 `格挡×5`。
  - 其他能力（净化、驱散、直接伤害、治疗等）：显示为紫色。
- 自定义 `combatRules` 会带触发器前缀（`命中后`、`战斗开始时`、`每回合结算后` 等）。
- 无任何能力的怪物不显示该区块。

预计计算读取同一缓存服务，地图显伤和怪物手册不能各自维护一份缓存。

## 14. 移除原临界计算

新战斗模式中，勇士攻击不再是单一数值，原有“增加几点攻击可以减少一回合伤害”的临界概念不再成立，因此需要移除原临界逻辑。

项目层处理范围：

1. 停止调用 `nextCriticals()`、二分临界和循环临界算法。
2. 删除或禁用项目中对 `_nextCriticals_useBinarySearch` 的覆写。
3. 怪物手册不再显示“临界”和“减伤”两列，替换为预计伤害、预计回合和攻击。
4. 怪物详情不再生成临界表和模仿临界计算器。
5. 地图图块不再绘制临界值。
6. 设置界面移除“临界显伤”开关。
7. `enemy.critical`、`enemy.criticalDamage` 不再参与项目逻辑。

遵守项目约束，不直接删除 `libs/` 中的引擎实现；在项目插件层覆盖显示和调用入口，使临界逻辑不再执行。

## 15. 战斗 UI

战斗面板至少包含：

- 勇士 HP、攻击和当前命中率。
- 怪物 HP、攻击、命中率和攻击 CD。
- 每把已摆放武器的图标、伤害范围、命中率和独立 CD。
- 全队奥义槽。
- 玩家和怪物的 Buff/Debuff 图标、层数、固定回合 CD。
- 1 倍、2 倍、4 倍和暂停按钮。
- 可折叠战斗日志。

武器 CD 和固定回合状态 CD 均从只读战斗快照渲染。渲染层不得直接修改战斗状态。

## 16. 战斗入口与结束流程

### 16.1 开始战斗

1. 保存并停止自动寻路。
2. 检查目标怪物和已摆放武器。
3. 执行现有 `beforeBattle` 和战前地图事件。
4. 自动存档。
5. 锁定地图操作并设置专用事件 ID。
6. 从背包、勇士、怪物和开局种子状态创建战斗状态。
7. 执行 `battleStart` 联动。
8. 启动模拟和渲染。

### 16.2 胜利

1. 停止战斗循环。
2. 将剩余勇士 HP 和实际受伤写回。
3. 更新战斗次数与受伤统计。
4. 复用统一胜利结算：金币、经验、战后特殊、怪物移除、支援怪、地图事件。
5. 清理临时战斗状态和 UI。
6. 解锁控制并继续自动寻路或事件队列。

### 16.3 失败

1. 勇士 HP 写为 0。
2. 停止战斗循环和 UI。
3. 不删除怪物，不发放奖励。
4. 调用现有失败流程。

## 17. 存档、录像与倍速

- 第一版不支持战斗中手动存档；读档回到战前自动存档。
- 战斗过程完全自动，不记录每一次攻击到录像路线。
- 录像使用相同开局种子、相同背包状态和相同事件顺序重新模拟。
- 背包录像只记录单次增量操作，不再在关闭背包、商店购买或商店赠予后记录完整的 `placed`、`inventory`、`unlockedCells` 快照；关闭背包本身不写录像动作。
- 武器由待摆放区进入装备格记录为 `bp:{instanceId}:i`；从装备格移回待摆放区记录为 `bp:{instanceId}:o`；出售记录为 `bp:{instanceId}:s`。
- 武器摆放、移动或旋转结束后记录为 `bp:{instanceId}:m:{rotationIndex}:{logicalX}:{logicalY}`。`rotationIndex` 的 `0/1/2/3` 分别表示 `0°/90°/180°/270°`，坐标是旋转后武器包围盒最上方一行、最左侧格子的逻辑坐标。
- 解锁扩容格记录为 `bp:-1:{logicalX}:{logicalY}`，回放时同样校验扩容位置并消耗一个背包格子道具。
- 初始 `8×8` 背包的左上角是逻辑坐标 `(0,0)`。当前最大 `12×12` 网格相对初始区域向四周各外扩两圈，因此横纵逻辑坐标范围均为 `-2～9`；向上或向左扩容得到负坐标。
- `i` 动作不记录 `weaponId`。武器获取流程必须具有确定性，回放通过 `instanceId` 查找已经存在的武器；若找不到对应实例、目标位置不可摆放或扩容不合法，则该录像动作失败，不用快照兜底。
- 自定义武器商店复用样板商店“按选项序号回放”的方式，使用原有的 `choices:n` 格式记录交互：`choices:0` 表示刷新货架，`choices:1～5` 表示购买第 1～5 栏武器；免费赠予选择器使用 `choices:1～5` 表示选择第 1～5 栏。
- 购买或赠予时先记录并回放 `choices:n`，由它执行扣款、价格/货架更新并确定性创建武器实例；随后才执行该实例对应的 `bp:{instanceId}:i` 和 `bp:{instanceId}:m:...`。
- 回放打开商店或赠予选择器时不创建 DOM，但会按录制时相同的时机加载或生成候选货架，保证 `core.randShop()` 的调用次数、候选武器和 `instanceId` 自增顺序一致。
- 录像播放中仍禁止手动打开背包整理（`openBackpack` 直接拒绝）；布局变化由注册的 `bp` 回放动作逐条执行。
- 倍速按钮只影响现实时间倍率，不影响逻辑 Tick。
- 倍速选择使用全局本地设置保存；任意战斗切换到 1、2 或 4 倍后，后续战斗和页面刷新都默认沿用该倍速。
- 录像高速模式可不播放逐次动画，直接快速推进模拟器。
- 回放结束时可校验随机调用次数、最终 HP、战斗 Tick 和状态摘要，发现不一致立即报告回放错误。

## 18. 推荐模块划分

```text
project/randomStreams.js
  randShop/randBattle 独立确定性随机流

project/weapons.js
  武器基础属性、布局联动、combatRules

project/backpackBattleStatuses.js
  Buff/Debuff 注册表、图标、动态说明和周期规则

project/backpackBattleCore.js
  实际战斗调度、CD、奥义、状态和统一随机服务；不得依赖DOM或Canvas

project/backpackBattleRules.js
  实际战斗和预计模拟共用的纯公式与状态规则；随机选择由外层 handlers 注入

project/backpackBattleEstimate.js
  主线程异步调度、缓存键、pending/ready/error缓存、任务去重和地图显伤接口

project/workers/backpackBattleEstimateWorker.js
  Web Worker入口；接收当前种子副本并执行单次真实随机序列预计模拟，不加载实际战斗随机服务

project/backpackBattleUI.js
  战斗面板、武器CD、状态图标、提示和倍速显示

project/plugins.js
  与魔塔事件、录像、状态栏和怪物手册的桥接

project/functions.js
  战前/战后事件及统一奖励结算
```

所有模块通过公开 API 交互，UI 不直接访问模拟器内部可变对象。

## 19. 建议公开 API

```text
core.plugin.backpackBattle.start(enemyId, x, y, options)
core.plugin.backpackBattle.pause()
core.plugin.backpackBattle.resume()
core.plugin.backpackBattle.setSpeed(speed)
core.plugin.backpackBattle.getSnapshot()
core.plugin.backpackBattle.fastForward()
core.plugin.backpackBattle.stop()

core.plugin.backpackBattleEstimate.request(enemyId, x, y, floorId)
core.plugin.backpackBattleEstimate.peek(enemyId, x, y, floorId)
core.plugin.backpackBattleEstimate.clear()
core.plugin.backpackBattleEstimate.bumpLayoutRevision()

core.randShop()
core.randShop(max)
core.randBattle()
core.randBattle(max)
```

`randShop` 和 `randBattle` 使用与 `core.rand` 相同的 Park-Miller 算法及参数语义，但分别持久化状态，互不推进。

`backpackBattleEstimate.request()` 立即返回当前状态快照：`pending`、`ready` 或 `error`，不等待计算结束。地图绘制使用该状态显示“计算中”或缓存结果；Worker 返回后由预计伤害模块触发定向重绘。`peek()` 只读缓存，不创建新任务。

## 20. 测试与验收

### 20.1 时间与 CD

- 0.25 间隔武器在 100 Tick 内恰好攻击 4 次。
- 怪物默认在第 100 Tick 第一次攻击。
- 冰洁和激奏每层准确改变 1 Tick。
- 1 倍、2 倍、4 倍及掉帧补帧结果完全一致。

### 20.2 防御与伤害

- 勇士和怪物都不受防御影响，伤害为税前全额（扣除格挡后）。
- 格挡在伤害结算前抵扣，剩余层数正确保留。
- 小数伤害能够累计，不因逐次取整永久为 0。

### 20.3 固定回合状态

- 烧伤和再生只在 100、200、300……Tick 结算。
- 中途获得的状态不会在同一回合边界立即结算。
- 再生先按旧层数治疗，再减少 1 层。
- 图标 CD 与逻辑回合时钟完全同步。

### 20.4 命中与触发

- Miss 不造成伤害，也不触发任何附带效果。
- 黑暗正确降低双方命中率。
- 反射不会发生无限循环。
- 净化和驱散只在攻击命中后执行。

### 20.5 奥义

- 高扬、虚脱正确修改奥义获取。
- 100 奥义立即触发全部可攻击武器。
- 奥义攻击不改变原 CD、不获取奥义。
- 多次溢出奥义能按规则连续触发且不会无限递归。

### 20.6 随机与回放

- 相同种子、相同输入分别得到完全相同的战斗序列和商店抽取序列。
- 商店刷新不会推进 `__randBattle__`，实际战斗不会推进 `__randShop__`。
- 查看地图显伤和怪物手册不会改变实际随机序列。
- 录像倍速变化不改变随机调用次数。
- 代码中不存在影响玩法的 `Math.random()` 调用。

### 20.7 预计伤害与缓存

- 每次预计从当前 `__randBattle__` 的数值副本开始，只产生一个真实随机序列对应的预计受伤结果。
- 命中、上下限伤害、概率条件、净化、驱散、随机 Buff/Debuff 和随机消耗均按实际战斗逻辑计算。
- Worker 输入包含起始种子；局部随机调用不会读取或修改实际 `core.flag`。
- 相同输入与种子的预估必须和实际战斗得到相同的伤害、回合、随机调用次数和结束种子。
- 勇士预计死亡后继续模拟，怪物在 10000 回合内死亡时仍返回未截断的最终受伤数值。
- 恰好在第 10000 回合结束的战斗显示具体数值；只有需要超过 10000 回合才显示 `???`。
- 预计受伤大于当前 HP 时显示红色具体数值，不显示“不可战胜”或 `???`。
- 缓存未命中和 Worker 计算期间显示“计算中”。
- 预计模拟在 Web Worker 中执行，不阻塞主线程。
- 同一缓存键重复查询只计算一次。
- 相同缓存键的并发查询只提交一个 Worker 任务。
- 当前随机种子改变后生成新缓存键，不能复用旧种子的结果。
- 背包旋转、移动、增删武器后缓存立即失效。
- 同 ID 但最终属性不同的怪物不会错误共用缓存。
- 旧排列的迟到 Worker 结果不会覆盖当前排列缓存。
- Worker 错误显示“计算失败”，不会误显示 `???`。

### 20.8 临界移除

- 地图、怪物手册、详情页和设置中不再出现临界值。
- 新战斗和预计模拟不调用 `nextCriticals()`。

## 21. 实施顺序

1. 建立状态注册表、统一随机服务和纯战斗状态结构。
2. 实现整数 Tick、独立 CD 和普通攻击。
3. 实现奥义、Buff/Debuff、固定回合结算和命中后事件。
4. 实现无 UI 的确定性单元测试。
5. 将纯模拟核心拆为 Worker 可加载模块，实现当前种子的真实战斗预演、Web Worker 异步调度、任务去重和缓存。
6. 拆分现有战后伤害与通用奖励结算。
7. 接入实际战斗入口、胜负和录像快进。
8. 实现战斗 UI、全部状态图标、动态说明和 CD 遮罩。
9. 替换地图显伤和怪物手册内容。
10. 禁用并移除项目层全部临界计算入口。
11. 为具体武器补充 `combatRules` 并进行整塔回归测试。

## 22. 当前实现清单

本设计已在项目层完成接入，未修改 `libs/`。主要落点如下：

| 模块 | 实现文件 | 状态 |
|---|---|---|
| 武器唯一属性表与具体联动 | `project/weapons.js` | 已完成 |
| 商店与战斗独立确定性随机流 | `project/randomStreams.js` | 已完成 |
| Buff/Debuff 图标、排序、周期与动态说明 | `project/backpackBattleStatuses.js` | 已完成 |
| 状态、格挡、反射、净化和驱散公共规则（防御已舍弃） | `project/backpackBattleRules.js` | 已完成 |
| 实际战斗 Tick、独立 CD、统一随机、奥义和倍速 | `project/backpackBattleCore.js` | 已完成 |
| 局部种子真实预演核心 | `project/backpackBattleEstimateKernel.js` | 已完成 |
| Worker、LRU 缓存、并发去重和过期结果丢弃 | `project/backpackBattleEstimate.js`、`project/workers/backpackBattleEstimateWorker.js` | 已完成 |
| 战斗面板、状态图标、周期 CD、动态提示、日志和控制按钮 | `project/backpackBattleUI.js` | 已完成 |
| 战斗入口、胜负写回、地图显伤、怪物手册与临界停用 | `project/backpackBattle.js` | 已完成 |
| 背包排列变更失效和武器实例桥接 | `project/plugins.js` | 已完成 |
| 战后奖励、统计与怪物移除复用 | `project/functions.js` | 已完成 |

实现额外保证：

1. 未摆放任何可攻击武器时禁止启动战斗，避免无进展战斗永久挂起。
2. 战斗完成结算采用幂等订阅与结束回调双保险，防止结束瞬间的 UI 操作造成结算竞态。
3. 商店与战斗随机分别通过 `core.randShop()`、`core.randBattle()` 获取；项目层已移除影响玩法的 `Math.random()` 使用。
4. 预计模块与 Worker 只接收起始种子数值并在局部推进，绝不读取或回写实际随机 flag。
5. 预计命中、伤害、净化、驱散及其他随机规则与实际战斗使用相同算法和调用顺序。
6. 武器伤害不再折算到勇士状态栏攻击力，背包内每把武器只按自身最终属性和独立 CD 结算。
7. 项目层临界二分算法、临界调用和临界显示均已停用；怪物手册对应区域改为回合、攻击间隔和攻击。
8. 敌人支持 `combatRules` 和简化字段（`burn`、`ice`、`darkness`、`exhaustion`、`reflection`、`block`、`mark`、`highSpirit`、`excitation`、`regeneration`、`cleanse`、`dispel`、`attackInterval`、`hitRate`），在攻击命中后执行 `afterHit` 规则；预计模拟按局部种子真实判断命中并触发相同随机效果。

自动测试位于 `tests/backpackBattle.test.js`，覆盖防御移除、格挡、反射、周期状态、净化、动态间隔、奥义、Miss、开战联动、种子真实预演、预估与实战随机序列一致、10000 回合边界、随机种子缓存失效、并发去重和过期任务丢弃。浏览器回归已覆盖背包拾取与整理、缓存刷新、无武器保护、状态图标、暂停、倍速、胜利结算和地图怪物移除。

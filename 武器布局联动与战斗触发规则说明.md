# 武器布局联动与战斗触发规则说明

本文说明 `project/weapons.js` 中以下三个字段的写法：

- `synergyText`：只负责向玩家展示联动说明，不执行任何效果。
- `synergyRules`：根据背包摆放位置计算武器的最终属性。
- `combatRules`：在战斗过程中监听触发时机，并执行状态、伤害、治疗等效果。

武器属性的唯一配置源是 `project/weapons.js`。也可以在 `editor.html` 中选中物品，进入“图块属性”，在 `weapon` 分组里编辑这些字段。

在编辑器中填写规则时，应填写一个 JSON 数组。所有键名和字符串都建议使用双引号，不要写函数、注释或尾逗号。

---

## 一、最外层结构

一把武器可以同时拥有布局规则和战斗规则：

```json
{
  "synergyText": "周围每有一个食物，自身攻击+1；命中后使敌人烧伤+1",
  "synergyRules": [
    {
      "id": "foodAddsAttack",
      "trigger": "layout",
      "conditions": [],
      "effects": []
    }
  ],
  "combatRules": [
    {
      "id": "hitAddsBurn",
      "trigger": "afterHit",
      "conditions": [],
      "effects": []
    }
  ]
}
```

每条规则都由四部分组成：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 建议填写 | 规则的唯一英文标识，主要用于调试和属性加成记录 |
| `trigger` | 是 | 触发时机；布局规则和战斗规则支持的值不同 |
| `conditions` | 否 | 条件数组；同一条规则的所有条件均满足才会执行，即 AND 关系 |
| `effects` | 是 | 条件满足后依次执行的效果数组 |

`conditions` 省略或传空数组时，表示触发时无条件执行。

---

## 二、布局联动 `synergyRules`

### 2.1 基本模板

```json
[
  {
    "id": "exampleLayoutRule",
    "trigger": "layout",
    "conditions": [
      {
        "id": "targets",
        "kind": "nearby",
        "relation": "orthogonal",
        "directions": ["up", "down", "left", "right"],
        "distance": 1,
        "filter": {
          "weaponTypes": ["食物"]
        },
        "min": 1
      }
    ],
    "effects": [
      {
        "target": "self",
        "stat": "attack",
        "operation": "add",
        "value": 1,
        "perMatch": true,
        "conditionId": "targets"
      }
    ]
  }
]
```

布局计算会读取武器旋转后的实际占格。只要来源武器和目标武器存在一对占用格满足空间条件，就视为该目标匹配。

所有规则中的 `directions` 都以武器素材的 **0 度朝向** 为基准，而不是固定的屏幕方向。武器在背包中顺时针旋转后，联动区域和实际判定方向会同步旋转：90 度时 `up → right`，180 度时 `up → down`，270 度时 `up → left`。该规则同时适用于布局 `nearby`、`sideBox`，以及战斗 `nearbyCount`、`chance`、`linkedWeapon`、`triggerLinkedWeaponAttack`。

### 2.2 布局规则触发器

| `trigger` | 当前状态 | 说明 |
| --- | --- | --- |
| `layout` | 可直接使用 | 摆放、移动或旋转武器后，根据当前背包布局计算属性 |
| `battleState` | 预留 | 属性计算器能够识别，但当前战斗入口没有传入实时战斗状态，因此不会自动参与实际战斗属性计算 |

目前新规则应使用 `"trigger": "layout"`。不要直接照抄现有配置中的 `battleState` 样例，除非后续同时接入了动态属性重算。

### 2.3 布局条件：`nearby`

`nearby` 用于查找指定方向或区域内的武器：

```json
{
  "id": "nearbyFoods",
  "kind": "nearby",
  "relation": "orthogonal",
  "directions": ["up", "down", "left", "right"],
  "distance": 1,
  "filter": {
    "weaponTypes": ["食物"]
  },
  "min": 1,
  "max": 4
}
```

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `id` | 自动生成 | 条件标识；当效果使用 `conditionId` 时必须显式填写 |
| `kind` | 无 | 此处固定为 `nearby` |
| `relation` | `orthogonal` | `orthogonal` 表示正交方向直线；`sideBox` 表示某一侧的矩形区域 |
| `directions` | 四个方向 | 可选 `up`、`down`、`left`、`right` |
| `distance` | `1` | 最大扫描距离，最小按 1 处理 |
| `span` | 同 `distance` | 仅 `sideBox` 使用，表示矩形区域的横向或纵向宽度 |
| `filter` | 无限制 | 目标武器筛选器 |
| `min` | `1` | 至少匹配多少把 |
| `max` | 无上限 | 最多匹配多少把 |
| `includeSelf` | `false` | 是否允许来源武器自身参与匹配；空间条件通常不需要开启 |

`orthogonal` 要求目标格与来源格处于同一行或同一列，并且在指定距离以内。

`sideBox` 表示从来源武器的某一侧向外扫描矩形。例如右侧 4×4 区域：

```json
{
  "id": "rightFoods",
  "kind": "nearby",
  "relation": "sideBox",
  "directions": ["right"],
  "distance": 4,
  "span": 4,
  "filter": {
    "weaponTypes": ["食物"]
  },
  "min": 1
}
```

### 2.4 布局条件：`count`

`count` 不检查位置，只统计整个背包中满足筛选条件的武器：

```json
{
  "id": "sameWeapons",
  "kind": "count",
  "filter": {
    "itemIds": ["I423"]
  },
  "includeSelf": true,
  "min": 3
}
```

`includeSelf` 默认为 `false`。像“同名武器达到3把”这种规则通常应设为 `true`。

### 2.5 布局条件：`context`

`context` 用于比较外部传给属性计算器的上下文：

```json
{
  "id": "burnStacks",
  "kind": "context",
  "path": "enemy.burn",
  "operator": "gte",
  "value": 3
}
```

支持的比较运算：

| `operator` | 含义 |
| --- | --- |
| `eq` 或省略 | 等于 |
| `neq` | 不等于 |
| `gt` | 大于 |
| `gte` | 大于等于 |
| `lt` | 小于 |
| `lte` | 小于等于 |
| `in` | 实际值存在于 `value` 数组中 |

此条件属于高级接口。当前普通背包布局只提供 `trigger: "layout"`，没有自动提供 `enemy.burn` 等战斗数据。

### 2.6 布局筛选器 `filter`

布局规则支持以下筛选字段，各字段之间为 AND；同一个数组内部为“任意一个匹配即可”：

```json
{
  "itemIds": ["I420", "I421"],
  "weaponIds": ["waterDragonUmbrella"],
  "weaponTypes": ["枪", "食物"],
  "hasAttack": true
}
```

| 字段 | 说明 |
| --- | --- |
| `itemIds` | `project/items.js` 使用的物品ID，例如 `I420` |
| `weaponIds` | 武器定义内部的 `id`，例如 `waterDragonUmbrella` |
| `weaponTypes` | 武器类型；目标拥有数组中的任意类型即可 |
| `hasAttack` | `true` 只匹配可攻击武器；`false` 只匹配盾等无攻击物品 |

### 2.7 布局效果

```json
{
  "target": "matches",
  "conditionId": "nearbyFoods",
  "stat": "attack",
  "operation": "add",
  "value": 2,
  "perMatch": true,
  "maxStacks": 5
}
```

#### 效果目标 `target`

| 值 | 说明 |
| --- | --- |
| `self` 或省略 | 修改规则所属武器 |
| `matches` | 修改 `conditionId` 对应条件匹配到的武器 |
| `all` | 修改背包内全部武器，包括规则所属武器 |

使用 `matches` 时必须填写正确的 `conditionId`。

#### 可修改属性 `stat`

| 值 | 说明 |
| --- | --- |
| `attack` | 同时修改伤害下限和伤害上限，最常用 |
| `minAttack` | 只修改伤害下限 |
| `maxAttack` | 只修改伤害上限 |
| `hitRate` | 修改命中率；基础值范围通常为 0～1 |
| `attackInterval` | 修改攻击间隔，单位为回合；1 回合等于 100 Tick |
| `ultimateGain` | 修改每次普通攻击的奥义获取量 |

#### 运算 `operation`

| 值 | 说明 |
| --- | --- |
| `add` 或省略 | 原值加 `value × 生效次数` |
| `multiply` | 原值连续乘以 `value`；生效 N 次时结果为 `原值 × value^N` |
| `set` | 直接设置为 `value` |

#### 生效次数

- 默认生效 1 次。
- `"perMatch": true`：按 `conditionId` 条件匹配到的武器数量生效。
- `stacksFrom.kind: "matchCount"`：使用 `floor(匹配武器数 / divisor)` 作为次数；用于“每有 X 把联动武器生效一次”。
- `maxStacks`：限制最多生效次数。
- `stacksFrom.kind: "contextFloor"`：使用 `floor(上下文数值 / divisor)` 作为次数；属于需要外部上下文的高级用法。

例如“左边一格内每有 2 把刀，自身伤害+1”：

```json
{
  "target": "self",
  "stat": "attack",
  "operation": "add",
  "value": 1,
  "stacksFrom": {
    "kind": "matchCount",
    "conditionId": "leftKnives",
    "divisor": 2
  }
}
```

`conditionId` 指向同一条规则中的 `nearby` 或 `count` 条件。每把目标武器只统计一次，不会因为自身占多格而重复计数。匹配 5 把、`divisor: 2` 时生效 `floor(5 / 2) = 2` 次。

按匹配数量使攻击间隔每次减少 10%：

```json
{
  "target": "self",
  "stat": "attackInterval",
  "operation": "multiply",
  "value": 0.9,
  "perMatch": true,
  "conditionId": "nearbyItems",
  "maxStacks": 5
}
```

这里匹配 2 把时，最终间隔为 `基础间隔 × 0.9²`，不是直接减去 20%。

### 2.8 完整布局示例

#### 示例A：相邻每个食物使自身攻击+1

```json
[
  {
    "id": "linkedFoodAddsAttack",
    "trigger": "layout",
    "conditions": [
      {
        "id": "linkedFoods",
        "kind": "nearby",
        "relation": "orthogonal",
        "directions": ["up", "down", "left", "right"],
        "distance": 1,
        "filter": {
          "weaponTypes": ["食物"]
        },
        "min": 1
      }
    ],
    "effects": [
      {
        "target": "self",
        "stat": "attack",
        "operation": "add",
        "value": 1,
        "perMatch": true,
        "conditionId": "linkedFoods"
      }
    ]
  }
]
```

#### 示例B：左右可攻击武器攻击+3

```json
[
  {
    "id": "boostSideWeapons",
    "trigger": "layout",
    "conditions": [
      {
        "id": "sideWeapons",
        "kind": "nearby",
        "relation": "orthogonal",
        "directions": ["left", "right"],
        "distance": 1,
        "filter": {
          "hasAttack": true
        },
        "min": 1
      }
    ],
    "effects": [
      {
        "target": "matches",
        "conditionId": "sideWeapons",
        "stat": "attack",
        "operation": "add",
        "value": 3
      }
    ]
  }
]
```

---

## 三、战斗触发规则 `combatRules`

### 3.1 基本模板

```json
[
  {
    "id": "exampleCombatRule",
    "trigger": "afterHit",
    "conditions": [],
    "effects": [
      {
        "type": "applyStatus",
        "target": "opponent",
        "status": "burn",
        "stacks": 1
      }
    ]
  }
]
```

### 3.2 战斗触发器

| `trigger` | 触发时机 | 备注 |
| --- | --- | --- |
| `battleStart` | 战斗初始化后、首个 Tick 前 | 背包内每把武器的该规则执行一次 |
| `beforeAttack` | 该武器已满足攻击条件、进行命中与伤害判定前 | 用于按攻击来源临时修改本次税前伤害；不要在此添加“命中后”状态 |
| `afterHit` | 这把武器自身攻击命中后 | Miss 不触发；奥义附带的额外攻击命中也会触发 |
| `afterAttack` | 这把武器自身攻击后（无论命中与否） | Miss 也触发；用于"攻击时获得随机 buff"等效果 |
| `afterAllyHit` | 背包内任意武器命中后 | 每次命中会检查背包内所有武器的该规则 |
| `afterLinkedWeaponHit` | 任意武器命中后，检查它是否处于规则所属武器的联动区域 | 配合 `linkedWeapon` 条件和 `every` 表达“联动武器每命中 X 次” |
| `afterDealDamage` | 任意武器命中且实际伤害大于0 | 每次有效伤害会检查全部武器 |
| `beforeReceiveDamage` | 怪物命中、伤害结算前 | 用于概率减伤等修改本次受到的伤害（配合 `modifyReceivedDamage`） |
| `afterTakeDamage` | 怪物命中且勇士实际受到伤害后 | 格挡至0伤害时不触发 |
| `afterHeal` | 玩家自身 HP 回复后（回血>0） | 含再生、回血效果、饮料回血等一切来源；**规则内再次回血不会重复触发本触发器**（防递归），配合 `once: true` 实现"自身HP回复时仅触发一次" |
| `afterShieldEffect` | 本次被攻击周期内，有武器的被攻击类规则（`beforeReceiveDamage`/`afterTakeDamage`）实际发动过 | 配合 `nearbyShieldTriggered` 条件表达“附近盾牌的被攻击效果发动时” |
| `afterUltimate` | 一次奥义的全部武器额外攻击结束后 | 每次奥义执行一次；全部效果结束后奥义值强制清零 |
| `roundStart` | 每100 Tick的回合结算完成后 | 烧伤、再生先结算，然后触发 |
| `battleEnd` | 胜负确定时 | 在最终战斗结果生成前执行 |

“该武器攻击后附加效果”通常使用 `afterHit`，因为游戏规则要求攻击命中后才生效。

### 3.3 战斗条件

战斗条件支持 `status`、`counter`、`nearbyCount`、`sameNameCount`、`hpPercent`、`combatFlag`、`chance`、`linkedWeapon`、`nearbyShieldTriggered`、`buffStacks`、`hpLost`、`attackOrigin`。多个条件同时存在时必须全部满足。

#### 状态层数 `status`

```json
{
  "kind": "status",
  "target": "opponent",
  "status": "burn",
  "operator": "gte",
  "value": 3
}
```

#### 武器计数器 `counter`

```json
{
  "kind": "counter",
  "key": "hits",
  "operator": "gte",
  "value": 5
}
```

当前实际战斗维护的计数器：

| `key` | 说明 |
| --- | --- |
| `attacks` | 该武器发动攻击的次数，包括 Miss；攻击判定前先加1 |
| `hits` | 该武器命中的次数 |

计数器目前没有“重置”效果。因此可以写“第5次命中时触发一次”（`eq 5`），但不能仅靠现有规则稳定表达“每5次命中循环触发”。

#### 附近武器数量 `nearbyCount`

```json
{
  "kind": "nearbyCount",
  "directions": ["left", "right"],
  "distance": 1,
  "filter": {
    "weaponTypes": ["盾"],
    "hasAttack": false
  },
  "operator": "gte",
  "value": 1
}
```

战斗规则中的位置判断与布局规则 `nearby` 完全一致，使用严格的空间关系，不再使用曼哈顿大致方向：

- 默认（`orthogonal`）：目标格必须与来源格**同一行或同一列**，并且在指定方向上的 `1 ~ distance` 格范围内。例如 `left` 只接受正左方同行的格子，不接受左上、左下等斜向格子。
- `"relation": "sideBox"`：从来源武器某一侧向外扫描矩形区域，`distance` 表示向外延伸格数，`span` 表示矩形宽度（默认等于 `distance`）。

```json
{
  "kind": "nearbyCount",
  "relation": "sideBox",
  "directions": ["right"],
  "distance": 4,
  "span": 4,
  "filter": {
    "weaponTypes": ["盾"],
    "hasAttack": false
  },
  "operator": "gte",
  "value": 1
}
```

战斗筛选器支持：

- `weaponTypes`：任意类型匹配。
- `itemIds`：按物品ID匹配。
- `weaponIds`：按武器定义内部的 `id` 匹配。
- `hasAttack`：是否具有大于0的最大伤害。

战斗中的 `nearbyCount` 始终排除规则所属武器自身，目前不支持 `includeSelf`。

#### 同名武器数量 `sameNameCount`

```json
{
  "kind": "sameNameCount",
  "operator": "gte",
  "value": 3
}
```

统计**场上任意同名武器组的最大数量**（**含自身**），满足 `count 与 value 比较` 时条件通过。示例：`value: 3` 表示"场上存在 3 把及以上相同名称的武器"——**不要求与当前武器同名**（如规则填在 a 武器上，场上另有 3 把同名 b 也会触发），也不要求同类型。

可选字段 `groupBy`：
- 缺省：按**任意同名组最大数量**统计（上述行为）；
- `"groupBy": "weaponTypes"`：改为统计**与当前武器共享任意武器类型**的武器数（如 a 是"盾"，场上其他同为"盾"的 b 也计入）。`sameNameDamageBonus` 效果同样支持该字段。

```json
{
  "kind": "sameNameCount",
  "operator": "gte",
  "value": 3,
  "groupBy": "weaponTypes"
}
```

#### 生命值百分比 `hpPercent`

```json
{
  "kind": "hpPercent",
  "target": "self",
  "operator": "lte",
  "value": 0.3333
}
```

目标当前 HP 与**本场战斗最大血量**（`state.battleMaxHp`：进战斗时的血条满值快照，战斗中 `maxHp` 变化不影响分母）之比与 `value` 比较。示例：`target: "self"`、`operator: "lte"`、`value: 0.3333` 表示"自身 HP 掉到 1/3 以下"。

#### 战斗标记 `combatFlag`

```json
{
  "kind": "combatFlag",
  "key": "sacrificeDone",
  "negate": true
}
```

`state.combatFlags[key]` 为 `true` 时条件通过；`negate: true` 表示"标记**未**设置时通过"。配合 `setCombatFlag` 效果实现"某效果触发后关闭另一规则"。

#### 奥义值 `ultimatePercent`

```json
{
  "kind": "ultimatePercent",
  "operator": "gte",
  "value": 100
}
```

勇士当前奥义与 `value` 比较。示例：`operator: "gte"`、`value: 100` 表示"自身奥义 100% 时"。

#### 概率 `chance`

```json
{
  "kind": "chance",
  "base": 0.2,
  "nearbyBonus": 0.2,
  "directions": ["up", "down"],
  "distance": 1
}
```

判定概率 = `base` + 附近匹配武器数 × `nearbyBonus`（`directions`/`distance`/`filter` 规则与 `nearbyCount` 相同，始终排除自身），最终被限制在 0～1。上例即"20% 概率，每有一个配置在上下一格内的武器概率 +20%"。

- 实际战斗使用确定性随机服务掷骰（计入录像随机调用次数，可重放）。
- 预计伤害使用任务开始时的当前 `core.rand` 种子副本真实掷骰；同一种子下与立即开始实际战斗的判定一致，且不会推进游戏中的随机状态。
- 带空间字段的 `chance` 条件（`directions`/`distance`/`relation`/`span`）会像 `nearbyCount` 一样在背包界面显示联动范围；`relation: "sideBox"` + `span` 会按矩形区域显示。

#### 附近被攻击效果发动 `nearbyShieldTriggered`

```json
{
  "kind": "nearbyShieldTriggered",
  "directions": ["left", "right"],
  "distance": 1,
  "filter": { "weaponTypes": ["盾"] }
}
```

判断附近（`directions`/`distance`/`filter`，规则与 `nearbyCount` 相同，排除自身）是否存在"本次被攻击周期内发动过被攻击类效果"的武器。配合 `afterShieldEffect` 触发器表达"配置在左右一格内的盾牌的被攻击效果发动时"。上例即"左右一格内盾牌的被攻击效果发动时，自身格挡+1"：

```json
{
  "trigger": "afterShieldEffect",
  "conditions": [
    { "kind": "nearbyShieldTriggered", "directions": ["left", "right"], "distance": 1, "filter": { "weaponTypes": ["盾"] } }
  ],
  "effects": [
    { "type": "applyStatus", "target": "self", "status": "block", "stacks": 1 }
  ]
}
```

#### 累计HP损失 `hpLost`
- 本场战斗**所有来源**（被攻击、持续伤害、武器自伤）累计损失的血量（totalHpLost）与 value 比较。
- 例："自身HP累计损失20点后：本武器伤害+10" → `{ "kind": "hpLost", "target": "self", "operator": "gte", "value": 20 }` + `modifyAttackDamage` 效果。

#### 强化效果总层数 `buffStacks`

```json
{
  "kind": "buffStacks",
  "operator": "gte",
  "value": 10
}
```

判断自身所有强化效果（buff）的**层数之和**。配合 `consumeBuffs` 效果表达"攻击时：随机消耗10个强化效果"——`buffStacks` 保证层数足够，`consumeBuffs` 实际扣减：

```json
{
  "trigger": "beforeAttack",
  "conditions": [
    { "kind": "buffStacks", "operator": "gte", "value": 10 }
  ],
  "effects": [
    { "type": "consumeBuffs", "count": 10 },
    { "type": "addExtraAttack", "directions": ["right"], "distance": 4, "filter": { "weaponTypes": ["食物"] } }
  ]
}
```

#### 弱体层数 `debuffStacks`

```json
{
  "kind": "debuffStacks",
  "operator": "gte",
  "value": 10
}
```

判断目标身上所有弱体效果（debuff）的**层数之和**（默认 `target: self`）。示例：`value: 10` 表示"自身弱体效果 10 个以上"——配合净化/回血效果表达"自身弱体10个以上时，仅一次：净化10个Debuff/回复15HP/发动上方饮料"：

```json
{
  "trigger": "afterTakeDamage",
  "once": true,
  "conditions": [
    { "kind": "debuffStacks", "operator": "gte", "value": 10 }
  ],
  "effects": [
    { "type": "heal", "target": "self", "value": 15 },
    { "type": "cleanseOneDebuff", "target": "self" },
    { "type": "triggerWeaponEffects", "directions": ["up"], "distance": 1, "filter": { "weaponTypes": ["饮料"] } }
  ]
}
```

#### 本次命中的联动武器 `linkedWeapon`

`linkedWeapon` 用于判断本次命中的武器是否位于规则所属武器的指定区域，并且满足类型或 ID 筛选：

```json
{
  "kind": "linkedWeapon",
  "directions": ["left"],
  "distance": 1,
  "filter": {
    "weaponTypes": ["刀"]
  }
}
```

它只在带有 `hitWeapon` 上下文的命中类触发器中有意义，主要用于 `afterLinkedWeaponHit`。空间距离、筛选器、旋转规则与 `nearbyCount` 相同，并且不会把规则所属武器自身算作联动目标。

#### 攻击来源 `attackOrigin`

```json
{
  "kind": "attackOrigin",
  "value": "ultimate"
}
```

可用来源：

| `value` | 说明 |
| --- | --- |
| `normal` | 武器 CD 转好后发动的普通攻击 |
| `ultimate` | 奥义令背包所有武器立即发动的攻击 |
| `linked` | `triggerLinkedWeaponAttack` 立即触发的联动攻击 |
| `legacyExtra` | 旧 `triggerWeaponAttack` 触发的兼容攻击 |

通常只在 `beforeAttack` 中判断 `ultimate`，再用 `modifyAttackDamage` 修改本次攻击。

#### 每满足 X 次执行一次 `every`

`every` 写在战斗规则本身，不写在 `conditions` 或 `effects` 中：

```json
{
  "id": "leftKnifeEveryThreeHits",
  "trigger": "afterLinkedWeaponHit",
  "every": 3,
  "conditions": [
    {
      "kind": "linkedWeapon",
      "directions": ["left"],
      "distance": 1,
      "filter": { "weaponTypes": ["刀"] }
    }
  ],
  "effects": [
    {
      "type": "applyStatus",
      "target": "opponent",
      "status": "exhaustion",
      "stacks": 1
    }
  ]
}
```

只有本次攻击命中且所有条件都通过时才会给这条规则的计数器加1。达到 `every` 后执行一次效果并保留余数。计数按“规则所属武器 + 规则ID”分别保存；联动区域内多把匹配武器共同累计同一条规则的次数。

#### 战斗比较运算

支持 `gt`、`gte`、`lt`、`lte`、`neq`；省略或填写其他值时按“等于”处理。战斗条件不支持布局条件中的 `in`。

### 3.4 战斗目标 `target`

| 值 | 说明 |
| --- | --- |
| `player` | 勇士 |
| `enemy` | 怪物 |
| `opponent` | 规则来源方的对手；当前玩家武器使用时即怪物 |
| `self`、省略或其他值 | 规则来源方；当前玩家武器使用时即勇士 |

为了便于以后扩展敌方武器，推荐使用 `opponent` 表示敌人、`player` 表示勇士，不要依赖未知字符串回退行为。

### 3.5 效果数值

普通固定数值可以写 `stacks` 或 `value`。同时存在时优先使用 `stacks`，都省略时默认为1。

```json
{
  "type": "applyStatus",
  "target": "opponent",
  "status": "burn",
  "stacks": 2
}
```

也可以动态读取附近武器数量：

```json
{
  "type": "applyStatus",
  "target": "opponent",
  "status": "burn",
  "stacksFrom": {
    "kind": "nearbyCount",
    "directions": ["up", "down", "left", "right"],
    "distance": 1,
    "filter": {
      "hasAttack": true
    },
    "multiplier": 2
  }
}
```

或读取某一方的状态层数：

```json
{
  "type": "heal",
  "target": "player",
  "stacksFrom": {
    "kind": "status",
    "target": "player",
    "status": "regeneration",
    "multiplier": 10
  }
}
```

### 3.6 战斗效果类型

| `type` | 主要字段 | 说明 |
| --- | --- | --- |
| `applyStatus` | `target`、`status`、数值 | 添加状态；Debuff会正常经过反射判定 |
| `applyRandomDebuff` | `target`、数值、`pool` | 从池中随机选取1个Debuff并添加；默认池为全部减益，可用 `pool` 限定；实际战斗消耗随机数 |
| `applyRandomBuffs` | `target`、`count`、`pool`、数值 | 随机 `count` 次，每次从增益池随机选取 1 个 buff 添加指定层数（**可重复**，同一 buff 多次出现时层数累加）；默认池为全部**非专属**增益（反射/格挡/高扬/激奏/再生，专属 buff 刻印/MP/狼皮不参与随机），可用 `pool` 限定；预计使用当前种子的局部副本执行同样的随机选择 |
| `nearbyRandomBuff` | `target`、`directions`、`distance`、`filter`、`every`、`pool`、数值 | **每有 `every` 个附近匹配武器，随机获得 1 个强化 buff**（复用 `applyRandomBuffs` 的随机逻辑与池）；**任意触发器可用**——`battleStart` 一次性结算，或 `afterHit` 每次命中结算（如"攻击命中时：每有1个上下左右一格内的动物，自身随机获得1个强化效果"） |
| `nearbyCleanseDebuff` | `target`、`directions`、`distance`、`filter`、`every` | **每有 `every` 个附近匹配武器，净化 1 个弱体状态**（重复随机净化；如"每有1个配置在上下左右一格内的食物，并净化1个debuff"） |
| `nearbyApplyStatus` | `target`、`status`、`stacks`、`directions`、`distance`、`filter`、`every` | **每有 `every` 个附近匹配武器，施加 `stacks` 层指定状态**（每次触发实时计算；如"被攻击时：上下左右一格内每配置一个盾牌，自身格挡+2/高扬+1"） |
| `removeStatus` | `target`、`status`、数值 | 移除指定状态层数 |
| `consumeStatus` | `target`、`status`、数值 | 与 `removeStatus` 相同，语义上用于消耗资源 |
| `modifyBattleMaxHp` | `target`、`value` | **自身最大生命值增加 `value`**（如"攻击命中时：自身最大HP+10"）。这里的"最大HP"指**进入战斗时的最大生命值 `battleMaxHp`**（`hpPercent` 条件与回血上限的分母），**不是样板自带的 hpmax**；同时同步本场战斗血条上限 `player.maxHp`，当前 HP 不补；可多次叠加 |
| `selfExtraAttack` | `value` | **仅本武器`攻击次数 +value`**（无条件，如暗黑被提·拟像"本武器攻击次数+5"；只对自身生效，不影响其他武器） |
| `nearbyMaxHpBonus` | `directions`、`distance`、`filter`、`every`、`value` | **每有 `every` 个附近匹配武器（directions/distance/filter），自身最大HP（`battleMaxHp`，同上语义）`+value`**（battleStart 一次性结算；如"上下左右一格内每配置1个乐器或动物，自身最大HP+3"；filter.weaponTypes 多类型为"任一匹配"） |
- **rotate: false（固定方向联动）**：效果/条件里加 `"rotate": false` 后，directions 为**固定世界方向**（不随武器旋转），如"无论是否旋转，上方一格内触发"；所有饮料类武器的联动已默认加此标记 |
- **triggerWeaponEffects 链式触发**：被触发的武器若也有 triggerWeaponEffects 会继续触发其附近匹配武器（竖放 5 个饮料，最下面触发时上面的依次全部触发）；用 context.chainVisited 防循环（含源头，重复目标跳过）；链式触发**不消耗目标的 once**（仅主动触发受"仅触发一次"限制） |
- **buffReached 触发器**：trigger 为 `"buffReached"` 的规则在**玩家获得强化时**检查（条件如 buffStacks≥10 满足即触发），不依赖攻击/被攻击——适合"拥有 N 层以上强化时触发"的被动效果（如威尔士天浆，无攻击间隔也可触发）；buffReachedActive 防递归，一次 buff 变化至多触发一轮 |
| `nearbyDamageSelf` | `directions`、`distance`、`filter`、`every`、`value` | **每有 `every` 个附近匹配武器，自身扣除 `value` HP**（直接扣血，不经过格挡/防御；如"上方横向三格和下方横向三格内每配置1个武器，自身HP-50"） |
| `nearbyChanceBonus` | `directions`、`distance`、`filter`、`value` | **给附近匹配武器的 `chance` 条件增加 `value` 概率**（如"∧内盾牌触发被攻击效果的概率+20%"：battleStart 登记，此后这些武器所有规则里 `chance` 条件掷骰时 +20%） |
| `cleanseAllDebuffs` | `target` | 目标身上所有Debuff各减少1层；数值字段无效 |
| `cleanseOneDebuff` | `target` | 净化 1 个弱体状态：随机选取 1 个 stacks>0 的 Debuff 移除 1 层；实战与预计使用相同算法和调用顺序 |
| `modifyAttackDamage`（按状态层数） | `operation`、`value`、`stacksFrom`、`status`、`target` | **本次攻击伤害修改按目标状态层数动态计算**：`stacksFrom: true` 时读取 `target`（默认 opponent）身上 `status` 的层数 × `value`（如奥义发动时：敌方每有1层冰结，本次伤害+2：value 2 + stacksFrom true + status ice + target enemy） |
| `dealMpConsumedDamage` | `multiplier` | 造成本次战斗中**累计消耗 MP 值 × multiplier** 的直接伤害（无视格挡）；应配合 `afterUltimate` 使用（奥义发动时） |
| `setInvincible` | `durationTicks` | 设置无敌：`durationTicks` 内（默认 500 Tick = 5 秒）本武器受到的伤害降至 0（格挡也不消耗）；配合 `once: true` 实现"每场战斗只能触发1次" |
| `repeatAttack` | `count` | 触发**该武器**立即再攻击 `count` 次（origin=ultimate、不消耗奥义值）；配合 `afterUltimate` + 概率条件实现"奥义发动时30%概率使该武器的攻击发动N次" |
| `sameNameDamageBonus` | `id`、`threshold`、`value`、`weaponTypes` | 同名武器数量驱动的伤害加成：本武器 `name` 相同的武器数 ≥ `threshold`（默认3）时伤害 +`value`（满足条件即生效，与数量无关）；按 `id` 幂等注册，攻击时实时计算 |
| `dispelBuff` | `target` | 实际战斗随机选择目标的一个Buff并减少1层 |
| `dispelRandomBuff` | `target` | 与 `dispelBuff` 相同；预计使用当前种子的局部副本随机选择，不修改实际种子 |
| `dispelBuffPercent` | `target`、`percent` | 目标的每一种Buff分别减少 `floor(当前层数 × percent)` 层；不使用随机数 |
| `dealDamage` | `target`、数值、`direct` | 造成伤害；`direct: true` 时无视格挡，否则按正常税前伤害结算（防御不参与战斗） |
| `heal` | `target`、数值 | 恢复HP，不超过最大HP |
| `damageSelf` | 数值 | **自身扣除 HP**（如"攻击时：自身hp-20"）；amount 为 `value`/`stacks`，最低 0，可致死 |
| `healPercent` | `value` | **按本场战斗最大血量百分比恢复**（如"恢复1/3的hp"：`value: 0.3333` → 恢复进战斗时满血量 × 0.3333，不超过最大HP） |
| `setCombatFlag` | `key` | 设置战斗标记 `state.combatFlags[key] = true`；配合 `combatFlag` 条件（`negate`）实现"某效果触发后关闭另一规则" |
| `disableUltimate` | 无 | **禁用奥义发动**：`state.ultimateDisabled = true`，奥义攒满 100 也不会发动（配合"奥义满转狼皮"等自循环规则） |
| `gainUltimate` | 数值 | 增加勇士奥义；当前实现不读取 `target` |
| `globalUltimateGainBonus` | `value` | **所有武器**每次攻击的奥义获得量 +`value`（多规则累加，如"所有武器奥义获得量+20"） |
| `buffGainCounter` | `id`、`every`、`damage` | **强化效果获得计数**：自身每获得 `every` 层强化效果（buff），对敌方造成 `damage` 点直接伤害（如"自身每获得10个强化效果时：对敌方造成10点伤害"）；跨规则跨来源累计，按 `id` 幂等注册 |
| `mpConsumeCounter` | `id`、`every`、`value` | **MP 消耗计数**：每消耗 `every` 点 MP，自身奥义 +`value`（如"每消耗10点MP：自身奥义+5"）；跨规则跨来源累计（黑之魅力消耗/`consumeStatus` 消耗 MP 均计入），按 `id` 幂等注册 |
| `triggerWeaponAttack` | 无 | 令规则所属武器立即额外攻击一次，不影响原CD，也不获取奥义 |
| `triggerLinkedWeaponAttack` | `linkedWeapon` | 令联动区域内所有匹配武器立即攻击一次；不重置目标武器CD，会结算目标武器的奥义获取 |
| `modifyAttackDamage` | `operation`、`value` | 只修改当前这一次攻击的税前伤害上下限；应配合 `beforeAttack` 使用 |
| `modifyWeaponStat` | 见下节 | 添加战斗期武器属性修正 |
| `statusDamageBonus` | `status`、`every`、`value`、`weaponTypes`、`target`、`scope` | 状态驱动的全局武器伤害加成：按 `id` 幂等注册，攻击时按当前状态层数实时计算 `floor(层数/every) × value`，不累积；`weaponTypes` 限定受影响武器，`target` 为 `enemy`/`opponent` 时读取敌方状态（默认读勇士）；**`status: "allDebuffs"` 时按目标阵营全部 debuff 层数合计**（如"敌方每有5个debuff，本武器伤害+1"）；`scope: "nearby"` 时仅作用于注册武器 `directions`/`distance` 范围内的武器（缺省不含自身，可配 `includeSelf: true`） |
| `statusIntervalBonus` | `status`、`every`、`value`、`weaponTypes`、`target` | 状态驱动的攻击间隔修正：按 `id` 幂等注册，间隔计算时按目标阵营当前状态层数实时计算 `floor(层数/every) × value` **回合**（`value: -1.9` 即减少1.9回合=190 Tick），不累积；`target` 为 `enemy` 时读取敌方状态（如"敌方每有10层冰结，自身间隔-1.9"） |
| `statusExtraAttack` | `status`、`every`、`value`、`target`、`weaponTypes` | 状态驱动的额外攻击次数：按 `id` 幂等注册，攻击时按目标阵营当前状态层数实时计算 `floor(层数/every) × value`（如"敌方每有10层冰结，自身攻击次数+1"）；每次攻击造成多次伤害、触发多次战斗联动，奥义获取只判定一次 |
| `statusHitRateBonus` | `status`、`value`、`target` | 状态驱动的命中率修正（**存在即生效**，与层数无关）：目标方拥有 `status`（层数≥1）时本次攻击命中率 +`value`（如"狼皮效果中：命中率+0.5"）；应配合 `beforeAttack` 使用，最终命中率夹在 0~1 |
| `goldMultiplier` | `value` | 击败敌人获得的金币增幅：多条规则按 `value` **累加**（`value: 1` 表示金币增加一倍，即 ×2）；建议配合 `battleEnd` 使用 |
| `goldBonus` | `value` | 战后获得金币**固定增加** `value` 点（多条规则累加）；建议配合 `battleEnd` 使用 |
| `consumeBuffs` | `count` | 随机消耗自身强化效果 `count` 层；预计按当前种子的局部副本执行相同随机选择；配合 `buffStacks` 条件表达"随机消耗10个强化效果" |
| `allExtraAttack` | `value` | **给所有武器`攻击次数 +value`**（如"所有武器奥义发动时的攻击次数+1"；extraAttackCount 永久保留，可随奥义发动叠加；每次攻击多造成一次伤害并触发联动） |
| `addExtraAttack` | `directions`、`distance`、`filter` | 使范围内匹配武器**发动次数+1**：该武器每次攻击造成多次伤害、触发多次战斗联动，但**奥义获取只判定一次** |
| `nearbyExtraAttack` | `directions`、`distance`、`relation`、`span`、`filter`、`every`、`value` | 附近武器数量驱动的额外攻击次数：注册武器范围内每有 `every` 个匹配武器，其攻击次数 +`value`（如"上下左右一格内每配置2个食物，本物品攻击次数+1"）；多次伤害/联动，奥义只判一次 |
| `nearbyThresholdExtraAttack` | `directions`、`distance`、`relation`、`span`、`filter`、`threshold`、`value` | 附近武器数量**阈值**驱动的额外攻击次数：附近匹配武器数 **≥ `threshold`** 时攻击次数 +`value`（**二进制**，与数量无关；如"上下左右一格内食物与动物总数达到5个以上时，本武器攻击回数+2"）；`filter.weaponTypes` 多类型按"任意类型即可"匹配 |
| `nearbyIntervalBonus` | `directions`、`distance`、`relation`、`span`、`filter`、`every`、`value` | 附近武器数量驱动的攻击间隔修正：注册武器范围内每有 `every` 个匹配武器，其攻击间隔 +`value` **回合**（如"上下左右一格内每有一个盾，本武器使用间隔-0.3"） |
| `nearbyIntervalPercentBonus` | `directions`、`distance`、`relation`、`span`、`filter`、`every`、`value` | 附近武器数量驱动的攻击间隔**百分比**修正：每有 `every` 个匹配武器，攻击间隔 **×(1-value)**（value 为小数、线性累加；如 0.1 = 每只附近动物 使用间隔-10%，2 只 -20%） |
| `nearbyDamageBonus` | `directions`、`distance`、`relation`、`span`、`filter`、`value` | 注册武器范围内每配置一个匹配武器，其攻击伤害 +`value`（如"右侧4*4内每配置一个食物，本武器伤害+3"） |
| `triggerWeaponEffects` | `directions`、`distance`、`filter` | 立马发动附近匹配武器的全部效果：忽略目标武器的 trigger/conditions/once 限制，不重置其 CD；配合 `suppressLinkage` 防止链式递归 |
| `guaranteeHit` | 无 | 本次攻击必定命中（无视黑暗等命中率修正）；应配合 `beforeAttack` 使用 |
| `ignoreBlock` | 无 | 本次攻击无视目标格挡（`block`）抵扣；应配合 `beforeAttack` 使用 |
| `ignoreBlockAlways` | 无 | 本武器**常驻**无视目标格挡（`block`）抵扣（无需每次 `beforeAttack` 设置） |
| `modifyReceivedDamage` | `operation`、`value` | 修改本次受到的伤害（`add`/`multiply`/`set`，如 `add -7` 降低7点）；应配合 `beforeReceiveDamage` 使用 |
| `modifyUltimate` | `operation`、`value`、`target` | 修改奥义值：`operation` 支持 `add`/`percent`/`set`，`percent` 时 `value: 0.1` 表示 +10%、`-0.1` 表示 -10%；`target: "enemy"` 修改敌方奥义 |

> 规则级 `once: true`：整场战斗仅触发一次（记录在所属武器上）。与 `every` 独立，可同时使用。

#### 击败金币增幅

```json
{
  "trigger": "battleEnd",
  "effects": [{ "type": "goldMultiplier", "value": 1 }]
}
```

语义：击败敌人结算金币时，在原有基础上**增加 `value` 倍**。上例 `value: 1` 即"敌人金币增加一倍"（实际获得 ×2）。多条规则**累加**：两把"增加一倍"的武器叠加为 ×3（`1 + 1 + 1`），不是累乘的 ×4。`value` 可为小数（如 `0.5` 表示增加 50%，即 ×1.5）。与幸运金币道具的 ×2 叠加，与经典结算兼容（没有背包乱斗战斗结果的战斗不受影响）。

#### 一场战斗仅一次 + 立即发动附近武器效果

```json
{
  "trigger": "battleStart",
  "once": true,
  "effects": [
    { "type": "heal", "target": "self", "value": 10 },
    {
      "type": "modifyWeaponStat",
      "stat": "attack",
      "operation": "add",
      "value": 2,
      "weaponTarget": "all",
      "durationTicks": 500
    },
    {
      "type": "triggerWeaponEffects",
      "directions": ["up"],
      "distance": 1,
      "filter": { "weaponTypes": ["饮料"] }
    }
  ]
}
```

上例即"一场战斗仅可发动一次：回复10HP，5秒内所有武器伤害+2，立马发动上方一格内的饮料类武器的效果"：

- **`once: true`**：整场战斗只触发一次（按武器记录，与 `every` 独立；被 `triggerWeaponEffects` 立即发动的目标武器规则不受自身 once 限制）。
- **5 秒**：`durationTicks: 500`（1 回合 = 100 Tick = 1 秒），`weaponTarget: "all"` 作用于所有武器，`stat: "attack"` 同时修正伤害上下限；到期自动失效。
- **`triggerWeaponEffects`**：立马执行 `directions`/`distance`/`filter` 匹配武器的**全部 combatRules 效果**——忽略目标武器的 trigger/conditions/once 限制（即"不受其他武器只发动一次的限制"），只执行效果不发起攻击，因此**不重置目标武器 CD**；内部以 `suppressLinkage` 防止链式递归（被发动武器的 `triggerWeaponEffects`/`triggerWeaponAttack` 等联动类效果不会再次触发）。

#### 状态驱动的武器伤害加成

```json
{
  "type": "statusDamageBonus",
  "id": "markBonus",
  "status": "mark",
  "every": 10,
  "value": 5,
  "weaponTypes": ["精灵"]
}
```

语义：目标阵营每拥有 `every` 层 `status` 状态，`weaponTypes` 匹配的武器攻击伤害上下限各增加 `value`。上例即"自身每拥有10层刻印，所有精灵类武器攻击伤害 +5"。

- 建议用 `battleStart` 触发器注册一次；按 `id` 幂等（重复注册只保留最新配置，不会累积多条）。
- 加成在每次攻击时按**当前**状态层数实时计算：刻印 10～19 层加 1 份（+5），20～29 层加 2 份（+10），依此类推；层数下降时加成自动减少，天然可逆。
- 省略 `weaponTypes` 时作用于所有武器。
- `target` 为 `enemy`（或 `opponent`）时读取**敌方**状态，例如"敌方每有2层火伤，本武器伤害+1"：

```json
{
  "type": "statusDamageBonus",
  "id": "burnBonus",
  "target": "enemy",
  "status": "burn",
  "every": 2,
  "value": 1
}
```

- 与 `modifyWeaponStat` 的区别：后者写入武器属性修正并随时间累计/过期，适合一次性或限时强化；本类型适合"由状态层数驱动的常驻被动"，实战与预计伤害行为一致。

`scope: "nearby"` 可将加成限定到注册武器附近的武器，例如"敌方每有5层火伤，配置在上下一个内的物品伤害+1"：

```json
{
  "type": "statusDamageBonus",
  "id": "nearbyBurnBonus",
  "scope": "nearby",
  "target": "enemy",
  "status": "burn",
  "every": 5,
  "value": 1,
  "directions": ["up", "down"],
  "distance": 1
}
```

该模式默认**不含注册武器自身**（只作用于上下格内的其他武器）；如需包含自身加 `"includeSelf": true`。

#### 状态驱动的攻击间隔修正

```json
{
  "type": "statusIntervalBonus",
  "id": "iceInterval",
  "target": "enemy",
  "status": "ice",
  "every": 10,
  "value": -1.9
}
```

语义：目标阵营每有 `every` 层 `status` 状态，本武器攻击间隔增加 `value` **回合**（`value` 可为负，如 `-1.9` 表示减少1.9回合 = 190 Tick）。上例即"敌方每有10层冰结，自身间隔-1.9"。层数不足 `every` 时不计，层数下降自动恢复；间隔最低被限制为 1 Tick。

```json
{
  "trigger": "afterHit",
  "effects": [
    { "type": "applyStatus", "target": "opponent", "status": "ice", "stacks": 2 }
  ]
}
```

配合 `afterHit` 施加冰结，即可实现"攻击命中时：敌方冰结+2；敌方每有10层冰结，自身间隔-1.9"。

#### 附近武器数量驱动的额外攻击次数

```json
{
  "type": "nearbyExtraAttack",
  "id": "nearbyFoodExtra",
  "directions": ["up", "down", "left", "right"],
  "distance": 1,
  "filter": { "weaponTypes": ["食物"] },
  "every": 2,
  "value": 1
}
```

语义：注册武器 `directions`/`distance`（或 `relation: "sideBox"` + `span` 矩形区域）范围内**每有 `every` 个匹配武器**，其攻击次数增加 `value`（每次攻击造成多次伤害、触发多次战斗联动，**奥义获取只判定一次**），数量减少自动恢复。上例即"上下左右一格内每配置2个食物，本物品攻击次数+1"。

"被攻击时，配置在上下左右一格内每有一个盾：本武器伤害+2、使用间隔-0.3"——用 `beforeReceiveDamage` 触发器（怪物攻击时所有武器收到）配合 `nearbyDamageBonus` 与 `nearbyIntervalBonus`：

```json
[
  { "trigger": "afterHit",
    "effects": [{ "type": "applyStatus", "target": "opponent", "status": "darkness", "stacks": 1 }] },

  { "trigger": "beforeReceiveDamage",
    "effects": [
      { "type": "nearbyDamageBonus", "id": "shieldDmg",
        "directions": ["up", "down", "left", "right"], "distance": 1,
        "filter": { "weaponTypes": ["盾"] }, "value": 2 },
      { "type": "nearbyIntervalBonus", "id": "shieldInterval",
        "directions": ["up", "down", "left", "right"], "distance": 1,
        "filter": { "weaponTypes": ["盾"] }, "value": -0.3 }
    ] }
]
```

注意：黑暗状态 id 为 `darkness`。

组合示例："攻击命中时：敌方格挡和反射-8层，强化效果随机驱散1个；上下左右一格内每配置2个食物，本物品攻击次数+1；本武器的伤害无视敌方的格挡"：

```json
[
  { "trigger": "afterHit",
    "effects": [
      { "type": "removeStatus", "target": "opponent", "status": "block", "stacks": 8 },
      { "type": "removeStatus", "target": "opponent", "status": "reflection", "stacks": 8 },
      { "type": "dispelBuff", "target": "opponent" }
    ] },

  { "trigger": "battleStart",
    "effects": [
      { "type": "nearbyExtraAttack", "id": "nearbyFoodExtra",
        "directions": ["up", "down", "left", "right"], "distance": 1,
        "filter": { "weaponTypes": ["食物"] }, "every": 2, "value": 1 }
    ] },

  { "trigger": "battleStart",
    "effects": [{ "type": "ignoreBlockAlways" }] }
]
```

#### 狼皮 buff

狼皮是 kind 为 `buff` 的特殊状态（id `wolfSkin`），具有以下语义：

- **专属 buff**（`exclusive: true`）：不可被驱散（`dispelBuff`/`dispelBuffPercent`/`dispelLastBuff` 均跳过），不参与随机 buff 池（`getAllBuffIds` 排除）
- **不可叠加**：每次施加最多设 1 层（已存在则不入栈）；层数仅控制持续时间
- **每秒减 1 层**：通过 `periodic: true, periodTicks: 100` 在 `settlePeriodicStatuses` 结算
- **存在即生效**：剑/斧武器攻击次数 +1、伤害 +5（与层数无关，仅要求狼皮存在）
- **弱体无效**：敌方对本武器施加的 Debuff 在 `applyStatus` 中被狼皮吸收（不反弹、不入栈）

> 专属 buff（`exclusive: true`）目前为：刻印（`mark`）、MP（`mp`）、狼皮（`wolfSkin`）、黑之魅力（`blackCharm`）。它们均不可被驱散、不可被随机 buff 随机出。

#### 黑之魅力 buff

```json
{
  "id": "blackCharm",
  "name": "黑之魅力",
  "kind": "buff",
  "stackable": true,
  "periodic": true,
  "periodTicks": 100,
  "exclusive": true,
  "iconPath": "project/images/status/blackCharm.svg",
  "description": function (stacks) {
 return "每秒反射+1/再生+1/格挡+1，每秒消耗4层MP；MP 不足时解除该效果（专属 buff 不可被驱散或随机出）";
  }
}
```

黑之魅力是**专属 buff**（`exclusive: true`），每周期（`settlePeriodicStatuses`，1 秒 = 100 Tick）自动结算：

- **每周期反射 / 再生 / 格挡 各 +1 层**（对自身）
- **每周期消耗 4 层 MP**（`mp` 专属指示物）
- **MP < 4 时直接解除黑之魅力**（不消耗 MP，也仍会施加反射/再生/格挡 各 +1）
- 专属：`dispelBuff`/`dispelBuffPercent`/`dispelLastBuff` 均跳过；`getAllBuffIds`（随机 buff 池）排除

图标 SVG：`project/images/status/blackCharm.svg`（黑色背景 + 紫水晶 + "黑" 字 + 黄色 "3" 角标）。

> **MP 图标**：`project/images/status/mp.svg`——圆形黑紫色渐变背景 + 居中白色 "MP" 字样，**无角标**。

"战斗开始时自身MP+5 / 命中时MP+3、回血3、再生+1、净化一个弱体 / 奥义发动时按累计消耗MP造成伤害 / 黑之魅力中攻击时5秒无敌（once）" 完整配置：

```json
[
  { "trigger": "battleStart",
    "effects": [{ "type": "applyStatus", "target": "self", "status": "mp", "stacks": 5 }] },

  { "trigger": "afterHit",
    "effects": [
      { "type": "applyStatus", "target": "self", "status": "mp", "stacks": 3 },
      { "type": "heal", "target": "self", "stacks": 3 },
      { "type": "applyStatus", "target": "self", "status": "regeneration", "stacks": 1 },
      { "type": "cleanseOneDebuff", "target": "self" }
    ] },

  { "trigger": "afterUltimate",
    "effects": [{ "type": "dealMpConsumedDamage", "multiplier": 1 }] },

  { "trigger": "afterAttack",
    "conditions": [{ "kind": "status", "target": "self", "status": "blackCharm", "operator": "gte", "value": 1 }],
    "once": true,
    "effects": [{ "type": "setInvincible", "durationTicks": 500 }] }
]
```

说明：

- MP 的消耗（黑之魅力每秒 4 层等）会累计到 `state.mpConsumedTotal`，`dealMpConsumedDamage` 按其 × `multiplier` 造成直接伤害（无视格挡）。
- `setInvincible` 设置玩家无敌至 `tick + durationTicks`（默认 5 秒 = 500 Tick），期间受到的伤害降至 0（格挡不消耗）；`once: true` 保证整场战斗仅触发一次。

"命中随机驱散敌方1个强化 + 自身反射+1 / 奥义发动30%概率(每把附近刀+10%)该武器攻击9次 / 无视格挡" 完整配置：

```json
[
  { "trigger": "afterHit",
    "effects": [
      { "type": "dispelBuff", "target": "opponent" },
      { "type": "applyStatus", "target": "self", "status": "reflection", "stacks": 1 }
    ] },

  { "trigger": "afterUltimate",
    "conditions": [
      { "kind": "chance", "base": 0.3, "nearbyBonus": 0.1,
        "directions": ["up", "down", "left", "right"], "distance": 1,
        "filter": { "weaponTypes": ["刀"] } }
    ],
    "effects": [{ "type": "repeatAttack", "count": 8 }] },

  { "trigger": "battleStart",
    "effects": [{ "type": "ignoreBlockAlways" }] }
]
```

说明：

- `repeatAttack` 是**额外**再攻击 `count` 次（奥义机制本身的 1 次攻击之外）；要"发动9次"配 `count: 8`。
- 概率条件 `chance`：`base + 上下左右 1 格内匹配武器数 × nearbyBonus`（上例 3 把刀 → 0.3 + 3×0.1 = 0.6），实际战斗掷骰判定。

"被攻击时同名≥3 → 格挡+5 + 驱散敌方强化 / 同名≥3 本武器伤害+5" 完整配置：

```json
[
  { "trigger": "beforeReceiveDamage",
    "conditions": [{ "kind": "sameNameCount", "operator": "gte", "value": 3 }],
    "effects": [
      { "type": "applyStatus", "target": "self", "status": "block", "stacks": 5 },
      { "type": "dispelBuff", "target": "opponent" }
    ] },

  { "trigger": "battleStart",
    "effects": [{ "type": "sameNameDamageBonus", "id": "sameNameAtk", "threshold": 3, "value": 5 }] }
]
```

说明：

- `sameNameCount` 条件统计与当前武器 `name` 相同的武器数（含自身）。
- `sameNameDamageBonus` 按 `id` 幂等注册；满足"同名 ≥ threshold"时该武器伤害 +`value`（二进制，与数量无关）；`weaponTypes` 可限定适用类型。

规则配置（挂在剑/斧武器上）：

```json
{
  "trigger": "battleStart",
  "effects": [
    { "type": "statusDamageBonus", "id": "wolfSkinAtk", "target": "self",
      "status": "wolfSkin", "every": 1, "value": 5,
      "weaponTypes": ["剑", "斧"] },
    { "type": "statusExtraAttack", "id": "wolfSkinExtra", "target": "self",
      "status": "wolfSkin", "every": 1, "value": 1,
      "weaponTypes": ["剑", "斧"] }
  ]
}
```

图标 SVG：`project/images/status/wolfSkin.svg`。

"狼皮效果中：命中率+0.5" 用 `statusHitRateBonus`（存在即生效、与层数无关）：

```json
{
  "trigger": "beforeAttack",
  "effects": [
    { "type": "statusHitRateBonus", "status": "wolfSkin", "value": 0.5 }
  ]
}
```

完整组合示例（攻击命中时奥义+10 / 每有1个上下左右一格内的物品 伤害+5、间隔-0.1 / 狼皮效果中命中率+0.5 / 该武器无视敌方格挡）：

```json
[
  { "trigger": "afterHit",
    "effects": [{ "type": "gainUltimate", "stacks": 10 }] },

  { "trigger": "battleStart",
    "effects": [
      { "type": "nearbyDamageBonus", "id": "nearbyDmg",
        "directions": ["up", "down", "left", "right"], "distance": 1, "value": 5 },
      { "type": "nearbyIntervalBonus", "id": "nearbyInterval",
        "directions": ["up", "down", "left", "right"], "distance": 1, "value": -0.1 }
    ] },

  { "trigger": "beforeAttack",
    "effects": [{ "type": "statusHitRateBonus", "status": "wolfSkin", "value": 0.5 }] },

  { "trigger": "battleStart",
    "effects": [{ "type": "ignoreBlockAlways" }] }
]
```

#### 状态驱动的额外攻击次数

```json
{
  "type": "statusExtraAttack",
  "id": "iceExtraAttack",
  "target": "enemy",
  "status": "ice",
  "every": 10,
  "value": 1
}
```

语义：目标阵营每有 `every` 层 `status` 状态，本武器攻击次数增加 `value`（每次攻击造成多次伤害、触发多次战斗联动，**奥义获取只判定一次**）。上例即"敌方每有10层冰结，自身攻击次数+1"，层数实时计算、层数下降自动恢复。配合 `afterHit` 施加冰结与 `ignoreBlock` 无视格挡：

```json
[
  { "trigger": "afterHit",
    "effects": [{ "type": "applyStatus", "target": "opponent", "status": "ice", "stacks": 2 }] },

  { "trigger": "battleStart",
    "effects": [{ "type": "statusExtraAttack", "id": "iceExtraAttack",
      "target": "enemy", "status": "ice", "every": 10, "value": 1 }] },

  { "trigger": "beforeAttack",
    "conditions": [{ "kind": "status", "target": "self", "status": "mark", "operator": "gte", "value": 5 }],
    "effects": [{ "type": "ignoreBlock" }] }
]
```

#### 附近武器数量驱动的伤害加成与发动次数

```json
{
  "trigger": "battleStart",
  "effects": [
    {
      "type": "nearbyDamageBonus",
      "id": "foodDamage",
      "directions": ["right"],
      "distance": 4,
      "filter": { "weaponTypes": ["食物"] },
      "value": 3
    }
  ]
}
```

上例即"右侧4*4内每配置一个食物，本武器伤害+3"：注册武器 `directions`/`distance`（或 `relation: "sideBox"` + `span` 矩形区域）范围内每有一个匹配武器，其攻击伤害 +`value`。

```json
{
  "trigger": "beforeAttack",
  "conditions": [
    { "kind": "buffStacks", "operator": "gte", "value": 10 }
  ],
  "effects": [
    { "type": "consumeBuffs", "count": 10 },
    { "type": "addExtraAttack", "directions": ["right"], "distance": 4, "filter": { "weaponTypes": ["食物"] } }
  ]
}
```

上例即"攻击时：随机消耗10个强化效果使右侧4*4内食物发动次数+1"。"发动次数+1"的武器每次攻击**造成多次伤害、触发多次战斗联动，但奥义获取只判定一次**（奥义不随发动次数翻倍）。

#### 随机弱体效果

```json
{
  "type": "applyRandomDebuff",
  "target": "opponent",
  "stacks": 1
}
```

从全部减益（冰洁/烧伤/黑暗/虚脱，按状态注册表 `order` 顺序）中随机选取 1 个并添加指定层数。可用 `pool` 字段限定候选池：

```json
{
  "type": "applyRandomDebuff",
  "target": "opponent",
  "stacks": 1,
  "pool": ["burn", "ice"]
}
```

实际战斗使用确定性随机服务随机选取（计入录像随机调用次数）；预计伤害从当前种子复制出局部随机流，按完全相同的候选池和调用顺序选择，但不会回写 `__rand__`。

#### 随机增益 buff

```json
{
  "type": "applyRandomBuffs",
  "target": "self",
  "count": 6,
  "stacks": 1
}
```

从增益池（默认全部**非专属** buff：反射/格挡/高扬/激奏/再生；专属 buff 刻印/MP/狼皮不参与随机）随机选取 buff 添加层数。`count` 表示**随机次数**：每次随机选取 1 个 buff 添加 `stacks` 层，可重复（同一 buff 多次出现时层数累加）。上例即"随机获得一个 buff 一层，随机六次"。可用 `pool` 限定候选：

```json
{
  "type": "applyRandomBuffs",
  "target": "self",
  "count": 2,
  "stacks": 1,
  "pool": ["block", "mark", "highSpirit"]
}
```

配合 `afterAttack` 触发器实现"攻击时（不命中也可触发）自身获得随机 buff"——`afterAttack` 在每次攻击后触发，Miss 也生效：

```json
{
  "trigger": "afterAttack",
  "effects": [
    { "type": "applyRandomBuffs", "target": "self", "count": 6, "stacks": 1 }
  ]
}
```

实际战斗每次随机选取消耗随机数（计入录像随机调用次数）；预计伤害使用局部种子逐次真实选取，因此可能重复获得同一个 buff，并与立即开始实战的结果一致。

#### 百分比驱散

```json
{
  "type": "dispelBuffPercent",
  "target": "opponent",
  "percent": 0.5
}
```

它会遍历敌人的全部 Buff，并对每个 Buff 独立计算驱散层数。例如层数分别为1、2、3、5时，50%驱散量分别为0、1、1、2。这里没有随机选择；实战和预计计算结果一致，也不会消耗随机数生成器。

#### 立即触发联动武器攻击

```json
{
  "type": "triggerLinkedWeaponAttack",
  "linkedWeapon": {
    "directions": ["up"],
    "distance": 1,
    "filter": {
      "weaponTypes": ["刀"]
    }
  }
}
```

目标按战斗快照中的稳定武器顺序依次攻击。每把目标武器的行为如下：

- 进行完整的命中、税前伤害、格挡和自身命中效果结算。
- 原 CD 不归零，也不会改变当前 CD 进度。
- 正奥义获取正常增加；负奥义获取正常检查并消耗。
- 命中后会计入 `afterLinkedWeaponHit` + `every` 的联动武器计数。
- 不再触发 `afterAllyHit` 等普通联动，也会忽略本次攻击自身的 `triggerLinkedWeaponAttack` / `triggerWeaponAttack`，防止递归联动。
- 如果负奥义消耗不足，本次立即攻击直接跳过，不排队；原 CD 仍保持不变。

#### 只修改本次攻击伤害

```json
{
  "id": "ultimateAddsFiveDamage",
  "trigger": "beforeAttack",
  "conditions": [
    {
      "kind": "attackOrigin",
      "value": "ultimate"
    }
  ],
  "effects": [
    {
      "type": "modifyAttackDamage",
      "operation": "add",
      "value": 5
    }
  ]
}
```

`operation` 支持 `add`、`multiply`、`set`。该效果直接修改本次攻击的伤害上下限，之后才进行随机伤害取值和格挡抵扣；不会写入武器永久属性，也不会影响下一次普通攻击。

#### 概率伤害加成与必定命中

```json
{
  "trigger": "beforeAttack",
  "conditions": [
    {
      "kind": "chance",
      "base": 0.2,
      "nearbyBonus": 0.2,
      "directions": ["up", "down"],
      "distance": 1
    }
  ],
  "effects": [
    { "type": "modifyAttackDamage", "operation": "add", "value": 30 }
  ]
}
```

上例即"攻击时：20%概率使该武器伤害+30，每有一个配置在上下一格内的武器，该发动概率+20%"（附近武器数通过 `directions`/`distance` 统计）。

```json
{
  "trigger": "beforeAttack",
  "effects": [
    { "type": "guaranteeHit" }
  ]
}
```

上例即"本武器攻击必定命中（不受黑暗效果影响）"：命中判定被强制为必中，无视命中率与黑暗减益；预计伤害同步按命中率 100% 计算。

```json
{
  "trigger": "beforeAttack",
  "conditions": [
    { "kind": "status", "target": "self", "status": "mark", "operator": "gte", "value": 5 }
  ],
  "effects": [
    { "type": "ignoreBlock" }
  ]
}
```

上例即"自身拥有5层以上刻印时，造成的伤害无视敌方格挡效果"：本次攻击跳过目标的格挡（`block`）抵扣，格挡层数不被消耗；预计伤害同步计算。

### 3.7 添加战斗期武器属性修正

```json
{
  "type": "modifyWeaponStat",
  "weaponTarget": "all",
  "stat": "attack",
  "operation": "add",
  "value": 2,
  "durationTicks": 500
}
```

| 字段 | 说明 |
| --- | --- |
| `weaponTarget` | `all` 修改全部武器；`nearby` 修改规则所属武器 + `directions`/`distance`/`filter` 匹配的附近武器（严格正交）；省略或其他值只修改规则所属武器 |
| `stat` | 实战建议使用 `attack`、`minAttack`、`maxAttack`、`attackIntervalTicks`、`attackInterval` |
| `operation` | `add`、`multiply`、`set`；省略为 `add` |
| `value` | 修正数值 |
| `durationTicks` | 持续Tick数；省略表示持续到战斗结束，100 Tick等于1回合 |

注意：

- `stat: "attack"` 会同时作用于伤害下限和上限。
- 修改攻击间隔可用 `attackIntervalTicks`（Tick 单位，`-10` 表示减少0.1回合），也可用 `attackInterval`（回合单位，`-0.1` 表示减少0.1回合）；即使快照已生成 `attackIntervalTicks`，`attackInterval` 的修正也会被换算为 Tick 叠加生效。
- 武器是否具备主动攻击能力只看定义中的原始 `attackInterval`：原始值大于0时，布局与战斗期修正后的下限为1 Tick；原始值为0或未配置时，任何间隔增加、减少、乘算或设置效果都不能将其改成非0。
- `modifyWeaponStat` 当前固定读取自身的 `value`，不会使用 `stacksFrom` 计算倍数。
- 实际命中率和奥义获取目前直接读取战斗快照，因此不要用此效果动态修改 `hitRate` 或 `ultimateGain`。

示例——"本武器每5次攻击：本武器和上下一格内的武器间隔-0.1"：

```json
{
  "trigger": "afterHit",
  "every": 5,
  "effects": [
    {
      "type": "modifyWeaponStat",
      "stat": "attackInterval",
      "operation": "add",
      "value": -0.1,
      "weaponTarget": "nearby",
      "directions": ["up", "down"],
      "distance": 1
    }
  ]
}
```

`every: 5` 表示每5次命中触发一次（Miss 不计入）；`weaponTarget: "nearby"` 同时作用于本武器与上下一格内（`up`/`down`、距离1）的武器，更远或其他方向的武器不受影响；`attackInterval -0.1` 换算为 10 Tick，且多次触发叠加（每5次命中再减0.1回合）。

### 3.8 可用状态ID

规则中的 `status` 必须填写英文ID：

| 中文 | `status` | 类型 | 实际效果 |
| --- | --- | --- | --- |
| 冰洁 | `ice` | Debuff | 每层使全部攻击间隔增加1 Tick |
| 烧伤 | `burn` | Debuff | 每100 Tick造成“层数×10”直接伤害 |
| 黑暗 | `darkness` | Debuff | 每层使命中率降低5% |
| 虚脱 | `exhaustion` | Debuff | 每层使奥义获取量降低2 |
| 反射 | `reflection` | Buff | 每层反射接下来1层Debuff |
| 格挡 | `block` | Buff | 每层抵扣1点税前伤害，未耗尽部分保留 |
| 刻印 | `mark` | Buff | 资源状态，本身没有其他效果 |
| 高扬 | `highSpirit` | Buff | 每层使奥义获取量增加2 |
| 激奏 | `excitation` | Buff | 每层使全部武器攻击间隔减少1 Tick |
| 再生 | `regeneration` | Buff | 每100 Tick恢复“结算前层数×10”HP，然后减少1层 |

### 3.9 负奥义获取量

武器的 `ultimateGain` 可以填写负数，表示该武器每次攻击需要消耗奥义。例如：

```json
{
  "ultimateGain": -8
}
```

攻击前会先计算高扬和虚脱修正后的最终奥义变化量：

```text
最终奥义变化 = 武器 ultimateGain + 高扬层数×2 - 虚脱层数×2
```

为兼容原有规则，基础 `ultimateGain >= 0` 的武器最多被虚脱降到0，不会因为虚脱反向变成消耗；只有布局计算后的基础 `ultimateGain < 0` 才启用负奥义攻击门槛。

若最终值小于0，则当前奥义必须 `>= 绝对值` 才能攻击：

- 奥义恰好等于消耗量时允许攻击，并减到0。
- 普通攻击奥义不足时，武器保持满 CD，不清零；之后任意武器补足奥义时会重新检查。
- 立即联动攻击奥义不足时只跳过本次，不排队，目标武器原 CD 不变。
- 奥义触发的全武器攻击也要支付负奥义消耗；正奥义获取仍按原规则不从奥义额外攻击中获得。
- Miss 仍视为发动过攻击，因此已经支付的负奥义不会退还；正奥义获取也照常结算。

### 3.10 完整战斗示例

#### 示例A：战斗开始时，周围每把武器使敌人烧伤+2

```json
[
  {
    "id": "nearbyWeaponsAddBurnAtStart",
    "trigger": "battleStart",
    "effects": [
      {
        "type": "applyStatus",
        "target": "opponent",
        "status": "burn",
        "stacksFrom": {
          "kind": "nearbyCount",
          "distance": 1,
          "filter": {
            "hasAttack": true
          },
          "multiplier": 2
        }
      }
    ]
  }
]
```

#### 示例B：该武器命中后，敌人冰洁+1

```json
[
  {
    "id": "hitAddsIce",
    "trigger": "afterHit",
    "effects": [
      {
        "type": "applyStatus",
        "target": "opponent",
        "status": "ice",
        "stacks": 1
      }
    ]
  }
]
```

Miss 时不会触发这条规则。

#### 示例C：受到有效伤害后，自身获得1层格挡

```json
[
  {
    "id": "takingDamageAddsBlock",
    "trigger": "afterTakeDamage",
    "effects": [
      {
        "type": "applyStatus",
        "target": "player",
        "status": "block",
        "stacks": 1
      }
    ]
  }
]
```

#### 示例D：命中后驱散敌人一个Buff

```json
[
  {
    "id": "hitDispelsBuff",
    "trigger": "afterHit",
    "effects": [
      {
        "type": "dispelRandomBuff",
        "target": "opponent"
      }
    ]
  }
]
```

#### 示例E：命中时临时强化全部武器5回合

```json
[
  {
    "id": "hitTemporarilyBoostsAllWeapons",
    "trigger": "afterHit",
    "effects": [
      {
        "type": "modifyWeaponStat",
        "weaponTarget": "all",
        "stat": "attack",
        "operation": "add",
        "value": 2,
        "durationTicks": 500
      }
    ]
  }
]
```

每次命中都会新增一条修正；持续时间重叠时会叠加。

#### 示例F：左边一格内的刀每命中3次，敌人虚脱+1

```json
[
  {
    "id": "leftKnifeEveryThreeHitsAddsExhaustion",
    "trigger": "afterLinkedWeaponHit",
    "every": 3,
    "conditions": [
      {
        "kind": "linkedWeapon",
        "directions": ["left"],
        "distance": 1,
        "filter": {
          "weaponTypes": ["刀"]
        }
      }
    ],
    "effects": [
      {
        "type": "applyStatus",
        "target": "opponent",
        "status": "exhaustion",
        "stacks": 1
      }
    ]
  }
]
```

联动范围中有多把刀时，它们的命中次数共同累计。Miss 不累计。

#### 示例G：该武器命中时，立即触发上方一格内所有刀攻击

```json
[
  {
    "id": "hitTriggersUpperKnives",
    "trigger": "afterHit",
    "effects": [
      {
        "type": "triggerLinkedWeaponAttack",
        "linkedWeapon": {
          "directions": ["up"],
          "distance": 1,
          "filter": {
            "weaponTypes": ["刀"]
          }
        }
      }
    ]
  }
]
```

来源武器 Miss 时不会触发；目标刀的这次攻击不会把目标刀自己的 CD 归零。

#### 示例H：命中后使敌人所有 Buff 各驱散50%

```json
[
  {
    "id": "hitDispelsHalfOfEveryBuff",
    "trigger": "afterHit",
    "effects": [
      {
        "type": "dispelBuffPercent",
        "target": "opponent",
        "percent": 0.5
      }
    ]
  }
]
```

---

## 四、布局规则与战斗规则如何选择

| 需求 | 应使用 |
| --- | --- |
| 根据背包位置永久修改本场战斗的基础攻击、间隔、命中率 | `synergyRules` |
| 战斗开始时添加烧伤、冰洁等状态 | `combatRules` + `battleStart` |
| 武器命中后附加状态或驱散 | `combatRules` + `afterHit` |
| 任意友方武器命中时响应 | `combatRules` + `afterAllyHit` |
| 指定联动区域内武器每命中X次触发 | `combatRules` + `afterLinkedWeaponHit` + `linkedWeapon` + `every` |
| 命中后立即触发区域内其他武器攻击 | `combatRules` + `afterHit` + `triggerLinkedWeaponAttack` |
| 仅强化奥义触发的这次攻击 | `combatRules` + `beforeAttack` + `attackOrigin` + `modifyAttackDamage` |
| 每100 Tick执行一次效果 | `combatRules` + `roundStart` |
| 只显示一句联动说明 | `synergyText`；注意它不会自动生成规则 |

同一个玩法可以拆成多条规则。例如“左右武器攻击+3，左右武器命中时自身格挡+1”需要：

1. 一条 `synergyRules`，负责给左右武器加攻击。
2. 一条或多条 `combatRules`，负责监听命中并添加格挡。

“左右武器命中时自身格挡+1”可以直接使用 `afterLinkedWeaponHit` + `linkedWeapon`；如果还要求每3次才触发，再在规则上添加 `"every": 3`。

---

## 五、当前限制与常见错误

1. `synergyText` 只是说明文字，不会自动产生任何效果。
2. `synergyRules` 的 `layout` 规则在进入战斗前计算；战斗过程中不会因状态变化自动重新计算。
3. `battleState` 布局触发器目前没有接入实际战斗属性快照，暂不应作为正式规则使用。
4. `afterHit` 只在命中后执行；Miss 不造成伤害，也不触发攻击附带效果。
5. `afterAllyHit`、`afterDealDamage` 等全体监听会让背包内每把拥有该规则的武器都执行一次，注意叠加数量。
6. 新规则优先使用 `triggerLinkedWeaponAttack`；它带有递归联动保护。旧 `triggerWeaponAttack` 仅用于兼容已有配置，不建议再新增。
7. 循环“每N次触发”请使用规则顶层的 `every`。它在条件通过后计数，触发后自动保留余数。
8. 只使用本文列出的条件类型和效果类型。未知战斗条件当前可能被当作通过，但不会产生预期逻辑。
9. `conditionId` 必须与条件的 `id` 完全一致，包括大小写。
10. 预计伤害会从当前 `__rand__` 复制局部种子并执行真实随机计算；不得调用 `core.rand()` 或把结束种子写回游戏 flag。
11. 新增会影响预计伤害的效果后，应同步检查 `project/backpackBattleEstimateKernel.js`，确保实际战斗和预计计算一致。

---

## 六、编辑器填写与检查清单

1. 在 `editor.html` 右侧素材区选中武器物品。
2. 打开“图块属性”，找到 `weapon` 分组。
3. `synergyText` 填写面向玩家的中文说明。
4. 在 `synergyRules` 或 `combatRules` 点击“编辑”，填入完整 JSON 数组。
5. 保存后重新加载游戏，使 `project/weapons.js` 的新配置重新载入。
6. 实际进入一场战斗，检查战斗日志、状态图标和最终属性。
7. 涉及预计伤害时，同时检查地图显伤结果。

保存前建议逐项确认：

- `trigger` 是否属于正确的规则系统。
- `conditionId` 是否存在且拼写一致。
- `status` 是否使用英文状态ID。
- 攻击间隔是否区分了"回合"和"Tick"。
- 需要命中才触发的效果是否使用 `afterHit`。
- 是否可能通过额外攻击形成无限触发。
- `synergyText` 描述是否与真正配置的规则一致。

---

## 七、敌人战斗属性配置

敌人也可以使用与武器相同的 `combatRules` 机制。敌人攻击命中玩家后，会执行其 `afterHit` 规则。规则中的 `target` 语义与武器对称，但方向相反：

| `target` 值 | 武器规则中含义 | 敌人规则中含义 |
| --- | --- | --- |
| `opponent` | 怪物 | 勇士 |
| `self` 或省略 | 勇士 | 怪物自身 |
| `player` | 勇士 | 勇士 |
| `enemy` | 怪物 | 怪物自身 |

### 7.1 简化字段

在 `project/enemys.js` 中，可以直接使用简化字段，系统会自动生成一条 `afterHit` 规则：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `burn` | 数值 | 每次攻击命中玩家，玩家获得对应层数烧伤 |
| `ice` | 数值 | 每次攻击命中玩家，玩家获得对应层数冰洁 |
| `darkness` | 数值 | 每次攻击命中玩家，玩家获得对应层数黑暗 |
| `exhaustion` | 数值 | 每次攻击命中玩家，玩家获得对应层数虚脱 |
| `reflection` | 数值 | 每次攻击命中后，自身获得对应层数反射 |
| `block` | 数值 | 每次攻击命中后，自身获得对应层数格挡 |
| `mark` | 数值 | 每次攻击命中后，自身获得对应层数刻印 |
| `highSpirit` | 数值 | 每次攻击命中后，自身获得对应层数高扬 |
| `excitation` | 数值 | 每次攻击命中后，自身获得对应层数激奏 |
| `regeneration` | 数值 | 每次攻击命中后，自身获得对应层数再生 |
| `cleanse` | true 或 1 | 每次攻击命中后，净化自身所有 Debuff 各 1 层 |
| `dispel` | 数值 | 每次攻击命中后，驱散玩家对应数量的 Buff（每次随机选一种） |
| `attackInterval` | 数值 | 攻击间隔，单位为回合（1 回合 = 100 Tick） |
| `hitRate` | 数值 | 基础命中率，范围 0～1 |

简化字段等价于在 `combatRules` 中写一条 `afterHit` 规则。例如 `"burn": 4` 等价于：

```json
{
  "trigger": "afterHit",
  "effects": [
    { "type": "applyStatus", "target": "opponent", "status": "burn", "stacks": 4 }
  ]
}
```

### 7.2 直接配置 combatRules

敌人也可以直接写 `combatRules` 数组，用于更复杂的效果（如 `battleStart`、`roundStart` 等其他触发器）。简化字段生成的规则和自定义 `combatRules` 会合并执行。

### 7.3 示例

```json
"magicDragon": {
  "name": "魔龙",
  "hp": 1500, "atk": 600, "def": 250,
  "attackInterval": 1.5,
  "hitRate": 0.8,
  "burn": 3
}
```

上述配置表示：魔龙每 1.5 回合（150 Tick）攻击一次，命中率 80%，每次命中给玩家施加 3 层烧伤。

### 7.4 敌人规则的限制

- 敌人的 `afterHit` 使用 `suppressLinkage: true`，不会触发 `afterAllyHit`、`afterLinkedWeaponHit` 等背包联动。
- 敌人不支持 `triggerWeaponAttack` 和 `triggerLinkedWeaponAttack`（没有背包武器可以联动）。
- 敌人的净化和驱散与武器完全一致：净化使自身所有 Debuff 各减少 1 层，驱散从敌方 Buff 中随机选择一种并减少 1 层。
- 预计伤害模拟同步计算敌人的真实命中和 `afterHit` 效果，随机调用顺序与实战一致，但只推进 Worker 内的局部种子。

---

## 八、背包逻辑坐标与录像动作

### 8.1 逻辑坐标

背包录像使用稳定的逻辑坐标，不直接使用最大网格数组中的内部下标：

- 初始背包是 `8×8`，初始区域左上角定义为 `(0,0)`，右下角为 `(7,7)`。
- 当前背包最大为 `12×12`，初始区域在最大网格内部从 `(2,2)` 开始。
- 内部坐标转逻辑坐标：`logicalX = internalCol - 2`，`logicalY = internalRow - 2`。
- 逻辑坐标转内部坐标：`internalCol = logicalX + 2`，`internalRow = logicalY + 2`。
- 向左或向上扩容时逻辑坐标为负数。当前最多向外扩两圈，所以横纵坐标范围都是 `-2～9`。

武器移动动作中的坐标，始终指武器完成旋转后，其占格包围盒“最上方一行、最左侧格子”的逻辑坐标。它不是武器图片中心、鼠标落点或旋转前锚点。

### 8.2 录像格式

| 操作 | 录像动作 | 说明 |
| --- | --- | --- |
| 武器进入装备格 | `bp:{instanceId}:i` | 武器从待摆放区进入装备格；随后用 `m` 记录最终角度和位置 |
| 武器移出装备格 | `bp:{instanceId}:o` | 只表示从装备格移回待摆放区，不表示出售或删除 |
| 武器出售 | `bp:{instanceId}:s` | 删除该实例并按当前固定售价结算金币 |
| 武器移动或旋转 | `bp:{instanceId}:m:{rotationIndex}:{logicalX}:{logicalY}` | 记录操作结束后的最终角度和包围盒左上角坐标 |
| 解锁扩容格 | `bp:-1:{logicalX}:{logicalY}` | 解锁指定逻辑格，回放时也消耗扩容道具 |

旋转编号固定为：

| `rotationIndex` | 角度 |
| --- | --- |
| `0` | `0°` |
| `1` | `90°` |
| `2` | `180°` |
| `3` | `270°` |

示例：

```text
bp:12:i
bp:12:m:1:-1:3
bp:12:o
bp:12:s
bp:-1:-2:4
```

其中 `bp:12:m:1:-1:3` 表示实例 `12` 旋转到 `90°`，旋转后包围盒左上角位于逻辑坐标 `(-1,3)`；`bp:-1:-2:4` 表示解锁逻辑坐标 `(-2,4)` 的扩容格。

### 8.3 回放约束

- `instanceId` 使用保存在 `core` flag `__backpack_instance_id__` 中的计数器生成，新游戏第一把武器为 `1`，之后逐次加一。
- `i` 不携带 `weaponId`。商店、赠予、拾取等武器获取过程必须是确定的，回放时只通过 `instanceId` 定位已生成的武器。
- 找不到 `instanceId` 说明获取流程或录像已经不一致，回放动作直接失败，不创建替代武器，也不使用 `weaponId` 补救。
- `m` 会校验所有目标占格已经解锁、没有越界并且不与其他武器重叠；校验失败则录像动作失败。
- `o` 只接受当前位于装备格内的实例；待摆放区旋转不单独记录，武器再次进入装备格时由后续 `m` 记录最终角度。
- 背包关闭、商店购买和商店赠予不再写入完整背包快照。背包布局使用上述 `bp` 增量动作；商店交互使用样板原有的 `choices:n` 动作。

### 8.4 商店选择录像

自定义武器商店沿用样板商店“记录选择序号、回放时重新执行选项”的逻辑。当前商店虽然是独立 DOM 界面，不是引擎的 `type: "choices"` 事件，但录像格式仍复用样板原有的 `choices:n`，由商店回放处理器在对应上下文中执行：

| 录像动作 | 普通武器商店 | 免费赠予选择器 |
| --- | --- | --- |
| `choices:0` | 刷新货架 | 无效 |
| `choices:1` | 购买第 1 栏武器 | 获得第 1 栏武器 |
| `choices:2` | 购买第 2 栏武器 | 获得第 2 栏武器 |
| `choices:3` | 购买第 3 栏武器 | 获得第 3 栏武器 |
| `choices:4` | 购买第 4 栏武器 | 获得第 4 栏武器 |
| `choices:5` | 购买第 5 栏武器 | 获得第 5 栏武器 |

购买或赠予的路线顺序必须是：

```text
choices:{栏位}
bp:{instanceId}:i
bp:{instanceId}:m:{rotationIndex}:{logicalX}:{logicalY}
```

回放 `choices:n` 时会执行与正常点击相同的扣款、购买次数、刷新次数、货架刷新和武器创建逻辑。商店界面本身不会在回放时显示，但打开商店或赠予选择器仍会按录制时相同的时机调用随机生成逻辑，以保证候选结果和实例 ID 一致。若栏位不存在、金币不足或赠予上下文不匹配，`choices:n` 回放直接失败。非商店上下文中的 `choices:n` 仍交由样板原有的事件选择逻辑处理。

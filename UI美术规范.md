# 碧蓝幻想 UI 美术规范

版本：1.0 · 2026-09-05。视觉基准：当前主界面与初始职业选择界面。后续调整 UI 时先读取本文件，直接复用 `project/fantasyUI.js`，不重新猜测边框画法。

## 1. 风格与材质

明亮的日式幻想冒险风格：天空蓝、透光的珍珠白、香槟金金属。背景保留天空与人物层次；面板像嵌入细金边的蓝白玻璃或羊皮纸。光泽来自局部反光和多层金属细线；装饰以菱形星芒、对称枝叶细线、内凹圆弧为主。

主界面保留动态 MP4 背景、碧蓝幻想透明标题及人物遮挡效果；**不添加主界面最外圈边框**。职业选择、成就等内容面板可以使用带中心徽记的完整金框。

## 2. 色彩与字体

| 用途 | 色值或渐变 | 使用规则 |
| --- | --- | --- |
| 深蓝正文 | `#214b70` | 蓝白浅底上的主文字 |
| 辅助文字 | `#58778b` | 条件、时间、辅助标签，保持可读性 |
| 金色基准 | `#dac58a` | 装饰线和分隔线 |
| 暗金底沿 | `#92754c` / `rgba(137,111,61,.60)` | 承托亮边，形成金属厚度 |
| 金属渐变 | `#f9e8ad` → `#dac18a` → `#fff8cf` → `#c6a466` → `#fff2b9` → `#d2b679` | 色标位置 0、.19、.42、.64、.82、1 |
| 象牙白反光 | `#fff8d8` | 金框内缘细线 |
| 蓝色按钮 | `#29537f` → `#467da6` → `#9bd1de` | 顶部深蓝、底部冰蓝；文字 `#fff5cf` |
| 金色主按钮 | `#ffedb7` → `#e8bd68` → `#f5d790` → `#fff0b9` | 色标 0、.36、.72、1；文字 `#70471f` |
| 悬停亮边 | `#ffe18a` + `#fffde4` | 金色外光、近白内反光 |
| 珍珠面板 | 浅蓝 `rgba(238,249,253,.96)` → `rgba(231,245,247,.98)` → 奶油白 `rgba(255,252,231,.98)` | 保证正文可读 |
| 蓝白简介面板 | 顶部 `rgba(96,152,201,.96)`，中下段同珍珠面板 | 职业简介、重要弹框 |

标题、职业名、按钮使用 `Noto Serif SC / Songti SC / SimSun / serif`，偏粗宋体，保留古典笔锋。正文使用 `Microsoft YaHei / sans-serif`，不额外依赖远程字体。DOM 标题约 28–34 px，卡片名 18–20 px，正文 13–15 px，辅助文字 11–12 px。Canvas 使用逻辑尺寸随游戏画布统一缩放。

数量、进度等数字使用 `Arial / Microsoft YaHei / sans-serif`，设置 `font-variant-numeric: lining-nums tabular-nums`。数字应等高、共用基线且等宽，避免 Georgia 默认旧式数字产生高低错落。

## 3. 金色边框：必须复用真实内凹弧线

四角是**以矩形顶点为圆心、向内容内侧挖入的四分之一圆弧**，不是普通的外凸 `border-radius`。直线在距顶点一个半径处结束，再沿内凹圆弧转向下一条边。四角镜像对称；缩放时重新计算路径，禁止将整张边框图不等比拉伸。

标准半径：Canvas 卡片 11–13 逻辑 px；DOM 卡片 14–16 CSS px；大面板 22–24 CSS px。每层路径半径为 `max(2, min(radius - inset*.45, width/4, height/4))`。

从外到内画五道金属边沿：

| 内缩量 | 线宽 | 颜色 |
| --- | --- | --- |
| .60 | 2.70 | 暗金 `rgba(137,111,61,.60)` |
| 1.05 | 1.65 | 多色香槟金渐变 |
| 2.35 | .65 | 象牙白 `#fff8d8` |
| 4.10 | .70 | `rgba(215,188,122,.92)` |
| 6.30 | .55 | `rgba(255,253,226,.8)` |

四角内侧添加弧形细线和小菱形；大面板可以加枝叶曲线。顶边中心徽记由暗金、浅金、蓝色、象牙白的嵌套菱形组成，两侧小卷叶镜像。小卡片避免过多枝叶挤压文字。装饰位于内容后方，不接收点击，不进入无障碍树。

## 4. 六边形按钮与交互

左右尖端、上下平边的长六边形。斜肩长度为按钮高度的 `.40`；金属边沿、象牙白反光、蓝色或金色内芯逐层内缩绘制。底部中心有柔和径向反光，四个斜肩有弧形小高光，两端有对称小菱形。

- 主要动作（开始冒险、确认选择）：金色，默认保留轻微暖光。
- 返回、关闭、次要入口：蓝色，横竖屏一致。
- Hover 与键盘聚焦：轮廓增加约 2.1 px 金色亮边、1.25 px 近白内线、10–15 px 局部金色辉光，不能仅改变文字或填充。
- 选中：更亮更厚的金边、柔和金底、明确的勾选/状态标签，不能只依赖色相。
- 未选中角色：立绘覆盖少量灰蓝，文字仍然清晰。未解锁成就只降低图标饱和度，不能把整张卡片和正文一起压暗。
- 支持鼠标、触摸和键盘；减少动态效果偏好下停用装饰动画。

## 5. 布局基准

**主界面**：动态天空背景，上方透明金蓝标题，中下部金色开始按钮，下方蓝色次要按钮组。留出人物区域，保留现有标题和人物遮挡关系，无最外圈边框。

**职业选择横屏**：左侧三张内凹金边立绘卡，右侧职业简介；下方中央金色确认，右侧蓝色返回；选中卡有“已选择”。实际职业为剑士、琴师、术士。

**职业选择竖屏**：上方大立绘、中间三个职业头像、下方蓝白简介面板，金色确认与蓝色返回。行走图使用配置原四向素材播放朝下行走帧。职业简介不显示“定位”行。

**成就页**：浅色遮罩保留背景气氛；中心大金框、宋体标题、蓝色返回；四项进度统计；宽屏双列成就卡，窄屏单列。内容内部滚动，外框、标题和返回保持固定。

**成就大图弹框**：同源内凹金框、蓝白面板、大图、成就名与等级、蓝色返回。**解锁提示**：右下角小型珍珠金框、金色“成就已解锁”、图标、等级与名称，保留自动退场和排队行为。

## 6. 直接复用公共组件

公共对象：`fantasyUI_6f31b8ea_7c4d_4b67_a215_03b247f8e903`，由 `main.pureData` 加载，所有插件开始安装前可用。

```js
var theme = fantasyUI_6f31b8ea_7c4d_4b67_a215_03b247f8e903;

// Canvas：与职业选择、标题按钮使用相同实现。
theme.drawArcFrameOn(ctx, {x: 20, y: 30, w: 380, h: 260}, {
  radius: 13, fill: theme.pearlGradient(ctx, {y: 30, h: 260}), crest: true
});
theme.drawHexButtonOn(ctx, {x: 140, y: 300, w: 160, h: 40},
  "确认选择", true /* 金色 */, hovered, 18);

// DOM：尺寸由页面 CSS 控制，内容仍为原生文字/按钮。
theme.decorate(panelElement, {radius: 24, ornate: true, crest: true});
theme.decorate(cardElement, {radius: 15, interactive: true, selected: unlocked,
  fill: unlocked ? "gold" : "pearl"});
theme.decorate(backButton, {button: true, gold: false});

// 移除弹框或重建列表前释放装饰，避免遗留观察器/事件监听。
theme.releaseTree(panelElement);
panelElement.remove();
```

DOM 装饰用透明 Canvas，按元素实际尺寸与设备像素比重绘，复用原始内凹弧框与六边形函数。通过 `ResizeObserver` 响应尺寸变化，无常驻绘制循环。先将元素挂入文档再装饰；正文留 16–24 px 内边距，徽记上方留 10 px。不要给装饰 Canvas 加 `position:relative`。

按钮装饰画布在四周额外扩展 24 CSS px，并同步扩大实际位图、平移绘制原点，为高亮外发光留出空间。按钮自身使用 `overflow:visible`；仅修改 CSS overflow 无法解决 Canvas 位图边缘裁切。扩展的装饰不接收点击，按钮本体尺寸与点击区域不变；滚动列表中的卡片不扩展，以免改变滚动范围。

成就页顶部四项统计同时作为等级筛选按钮：全部、青铜、白金、钻石。选中项使用暖金底、金色亮边和 `aria-pressed`；其余为珍珠底。固定筛选卡调用 `decorate` 时设置 `interactive: true, bleed: 20`，完整保留悬停辉光。切换分类后列表回到顶部，统计仍显示各等级的完整解锁进度；再次打开页面默认显示全部。

## 7. 武器图鉴与兵装档案

武器的共用样式维护在 `project/weaponUI.css`，由入口页面统一加载；`project/weaponCompendium.css` 只维护图鉴的页面布局。`weaponCardRenderer.js` 默认给所有武器卡片及详情应用 `weapon-ui-skin` 和内凹金框，商店、奖励、背包、配方表不再各自定义卡片主题。`previewOnClick: true` 只用于启用桌面点击大图查看档案，背包保留原来的拖拽交互。

页面使用珍珠蓝白大面板、内凹金框与顶部菱形徽记；顶部依次为标题、收集/通关进度、名称及类型/分组/排序/收集状态筛选。分组标题使用淡蓝底和细金分隔线。返回按钮统一蓝色六边形，保留完整悬停和键盘焦点辉光。

横屏为自适应多列兵装卡片，卡片高度随正文增长，避免固定高度截断长描述；竖屏为单列缩略图列表，点击名称区域展开属性，点击图像查看档案。已收集保留原彩色武器，未解锁为低对比轮廓及 `???`，已通关使用暖金底与“◆ 通关印记”。稀有度保留星级，不以彩色霓虹边框覆盖统一金边。占格棋盘使用浅蓝灰网格、淡金占格描边，联动区域仍为金色箭头高亮。

档案弹框在横屏左右排列武器占格与属性，竖屏上下排列，长内容只滚动正文；返回按钮放在独立底部区域。进度与战斗数字统一使用 Arial、`lining-nums tabular-nums`。移除或重建图鉴、卡片和弹框前调用 `backpackUiCommon.releaseWeaponUI`，同时释放边框与粒子观察器。

### 全局武器入口的复用方式

商店与奖励页使用珍珠蓝白大面板、内凹金框、菱形徽记。购买和领取按钮为黄色六边形，放在特殊效果下方的独立卡片底栏，长说明滚动时按钮保持可见；刷新和返回使用蓝色六边形。横屏多列，竖屏单列。面板高度由视口决定，头部与底部操作区固定，连续刷新不得改变刷新按钮的位置。横屏卡片高度按视口固定在 480–540px，长属性与效果说明在卡片内部滚动；竖屏摘要固定 158px，展开固定 360px，详情独立滚动。列表超过可用空间时滚动列表，按钮四周保留辉光留白。

合成页的材料槽、候选武器、配方和结果均使用相同凹角金框；结果及选中的可用配方用暖金底区分。合成按钮用金色，返回用蓝色。候选列表随合成结果刷新。背包棋盘及战斗武器阵列使用浅蓝网格、淡金描边；横屏背包详情使用常驻面板，竖屏点击后展开浮层，战斗中的悬浮详情沿用同一金框。

自定义武器面板调用 `backpackUiCommon.decorateWeaponSurface(element, options)`，参数沿用 `fantasyUI.decorate`：面板可用 `{ radius: 23, ornate: true, crest: true }`，返回按钮用 `{ button: true }`，主操作用 `{ button: true, gold: true }`。新增卡片直接使用 `weaponCardRenderer.createCard`，不要重复装饰同一元素；购买/领取类卡片指定 `actionPlacement: "footer"`，把操作按钮放在详情后的独立底栏。更新按钮文字调用 `setWeaponButtonLabel`，避免直接覆盖 `textContent` 删除装饰画布；重建列表或移除弹框前调用 `releaseWeaponUI(root)` 释放观察器。

装饰 Canvas 保持绝对定位，不参与布局；带金框的滚动区域应放在面板内容层。面板改为 `overflow: visible` 时，Grid 行必须设置合理的 `minmax(0, 1fr)`，防止长配方列表撑高整页。稀有度保留星级与统一金框。仅商店和盲盒的候选武器卡片开启粒子：3 星附蓝色星芒，4 星附紫金星芒，5 星附金色星尘，分别使用 18 / 26 / 38 颗粒子。星芒使用清晰的彩色轮廓、明亮内芯和光晕，主体约 18–24px，以 38–74px 的行程飘动；各武器的分布与动画错开，浅色背景上也应清楚可见。粒子仅沿卡片边缘浮动，不遮挡正文、不改变布局、不消耗游戏随机数；共用组件默认关闭，只有商店与盲盒通过 `rarityParticles: true` 开启；物品栏、图鉴、合成及详情不生成粒子。粒子离屏与后台暂停，移除卡片时释放。小尺寸减少粒子，系统选择减少动态效果时使用静态微光。

### 背包工作台布局

布局维护在 `project/backpackLayout.css`，在共用武器样式后加载，限定于背包根节点的 `data-layout="atelier"`。背包挂在 `gameGroup` 内，与 `outerUI` 的位置、尺寸一致，通过 `ResizeObserver` 跟随全局缩放。桌面逻辑尺寸按游戏原始尺寸的 1.5 倍排版，竖屏使用原始尺寸，内部整体按对应比例缩放；横竖屏样式由背包的 `data-compact` 决定，不依赖浏览器宽度。背景固定使用 `project/images/backpack_background.png`，保持天空城原图明亮色调。各区域复用 `fantasyUI` 的珍珠蓝白填充、多层内凹弧角金框；合成用金色六边形，返回、旋转、一键收回及战速用蓝色六边形。普通按钮逻辑高度为 32px，三个主要操作为 44px，并随全局缩放。

桌面布局为左侧背包棋盘与旋转栏、右侧武器详情；左下是横向物品栏和出售区，右下是战速、合成、一键收回。竖屏从上到下为战速/合成/一键收回、背包棋盘、横向物品栏。背包标题、格子数量与返回按钮在同一行垂直居中。顶部至少留 28px，底部至少留 32px，并兼容安全区；棋盘按剩余高度计算格子尺寸，初始背包及完全扩容均优先一屏容纳，极矮屏保留内部滚动及拖拽边缘自动滚动。物品栏仅横向滚动，逻辑高度 140px，卡片为 72×72px，图标和名称完整放入卡片；标题及筛选框避开弧角装饰。

竖屏详情仅在点击武器后显示，以双栏金框浮层覆盖物品栏及上方部分网格，底边与物品栏底边重合，不挤占常驻布局。长内容在浮层内部滚动，顶部保留蓝色“收起”按钮；点击空白处或开始拖拽会清除选择并收起详情。竖屏平时隐藏出售区，仅拖拽时在物品栏位置显示一条操作带：左侧 2/3 放回物品栏，右侧 1/3 出售，中间留 6px 间隙；显示范围与实际命中区一致，拖拽结束后恢复物品栏。

棋盘显示范围由已解锁格子及其全部可扩容外圈共同计算，不能为了匹配示意图删掉外圈。解锁后重新计算范围，但实例坐标、旋转、联动范围和占格规则保持不变。打开背包不默认选中武器；桌面悬停临时预览，点击固定选择，点击空白处同时清除详情、选中高亮和联动区域。选中武器显示金色占格高亮，占格边框和底色必须位于武器图片下方，透明交互热区位于最上方；详情复用真实属性、当前布局加成、特殊效果和可执行配方，提供大图与配方入口。出售文案读取实际配置，目前为每把 30 金币，不使用设计图中的 80%。

### 战斗界面布局

布局维护在 `project/battleLayout.css`，限定于 `.bb-sky-battle`，在共用武器样式之后加载。复用天空城背景、珍珠蓝白面板、多层内凹金框和六边形按钮；我方形象读取当前职业配置中的立绘，没有立绘时使用当前勇士行走图；敌方读取当前敌人的图块或大图单帧。不得照搬示意图的人物、怪物、等级、台词或虚构战斗数据。

横屏上方为双方对战区，两侧显示名称、生命、攻击与命中、状态和奥义，敌方下次攻击使用圆环倒计时；下方左侧为当前背包兵装，中间是战斗操作和日志，右侧为 DPS 统计。竖屏从上到下为顶部楼层与战斗时间、双方形象及状态卡、并排的兵装和 DPS、操作按钮、全宽日志。常见手机高度优先一屏，极矮屏允许整页纵向滚动；长日志和伤害排行在各自面板内滚动，不挤压操作区。兵装棋盘根据实际最大行列数完整容纳，保留占格、冷却填色、攻击反馈、联动预览与统一武器详情。

伤害排行按兵装实例统计普通攻击及直接效果造成的格挡后伤害，同名兵装分别列出；持续伤害等公共来源合并为“其他效果”。总 DPS 为本场累计伤害除以战斗秒数，零秒显示 0，暂停和战速不直接改变统计口径；每行显示累计伤害与占比。统计只增加展示数据，不改伤害公式、随机数或结算。日志使用真实 Tick 时间，支持全部、伤害、治疗、状态、奥义筛选，手动上翻时不抢回底部。

战速保留 0.25×、0.5×、1×、2×、3×、10×；宽屏平铺，窄屏使用选择框。返回和暂停/继续为蓝色六边形，“立即结算”为金色六边形并沿用现有快速模拟逻辑。状态图标、伤害排行与人物绘制复用缓存，背景不叠加黑色遮罩；移除战斗界面时释放金框及尺寸观察器。

## 8. 行为与验收约束

主界面与职业选择复用同一 MP4 节点，切换时不调用 `load()`、不归零 `currentTime`、不重新挂载。现有轻雾渐变顶部透明度 .04、中部 .12、底部 .47，不重新加深黑色遮罩。

只改表现层；保留成就定义、隐藏条件、解锁时间、存储键、音效、排队、游戏控制锁和焦点恢复。定制放在 `project/`，不修改 `libs/`。

每次扩展检查横屏、竖屏、小屏；内凹弧角与完整金属层次；文字不挤入装饰区；hover 金边高亮；选中/未选中明显；列表可滚动、弹框可关闭；动态背景持续播放。

后续需求可直接写：**“遵循 UI美术规范.md，复用 project/fantasyUI.js 改造某页面。”**

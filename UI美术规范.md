# 碧蓝幻想 UI 美术规范

版本：1.0 · 2026-09-05。视觉基准：当前主界面与初始职业选择界面。后续调整 UI 时先读取本文件，直接复用 `project/fantasyUI.js`，不重新猜测边框画法。

## 1. 风格与材质

明亮的日式幻想冒险风格：天空蓝、透光的珍珠白、香槟金金属。背景保留天空与人物层次；面板像嵌入细金边的蓝白玻璃或羊皮纸。光泽来自局部反光和多层金属细线；装饰以菱形星芒、对称枝叶细线、内凹圆弧为主。

主界面保留动态 MP4 背景、碧蓝幻想透明标题及人物遮挡效果；**不添加主界面最外圈边框**。职业选择、成就等内容面板可以使用带中心徽记的完整金框。

## 2. 色彩与字体

| 用途 | 色值或渐变 | 使用规则 |
| --- | --- | --- |
| 深蓝正文 | `#214b70` | 蓝白浅底上的主文字 |
| 辅助文字 | `#58778b` | 条件、时间、辅助标签，保持可读性 |
| 金色基准 | `#dac58a` | 装饰线和分隔线 |
| 暗金底沿 | `#92754c` / `rgba(137,111,61,.60)` | 承托亮边，形成金属厚度 |
| 金属渐变 | `#f9e8ad` → `#dac18a` → `#fff8cf` → `#c6a466` → `#fff2b9` → `#d2b679` | 色标位置 0、.19、.42、.64、.82、1 |
| 象牙白反光 | `#fff8d8` | 金框内缘细线 |
| 蓝色按钮 | `#29537f` → `#467da6` → `#9bd1de` | 顶部深蓝、底部冰蓝；文字 `#fff5cf` |
| 金色主按钮 | `#ffedb7` → `#e8bd68` → `#f5d790` → `#fff0b9` | 色标 0、.36、.72、1；文字 `#70471f` |
| 悬停亮边 | `#ffe18a` + `#fffde4` | 金色外光、近白内反光 |
| 珍珠面板 | 浅蓝 `rgba(238,249,253,.96)` → `rgba(231,245,247,.98)` → 奶油白 `rgba(255,252,231,.98)` | 保证正文可读 |
| 蓝白简介面板 | 顶部 `rgba(96,152,201,.96)`，中下段同珍珠面板 | 职业简介、重要弹框 |

标题、职业名、按钮使用 `Noto Serif SC / Songti SC / SimSun / serif`，偏粗宋体，保留古典笔锋。正文使用 `Microsoft YaHei / sans-serif`，不额外依赖远程字体。DOM 标题约 28–34 px，卡片名 18–20 px，正文 13–15 px，辅助文字 11–12 px。Canvas 使用逻辑尺寸随游戏画布统一缩放。

数量、进度等数字使用 `Arial / Microsoft YaHei / sans-serif`，设置 `font-variant-numeric: lining-nums tabular-nums`。数字应等高、共用基线且等宽，避免 Georgia 默认旧式数字产生高低错落。

## 3. 金色边框：必须复用真实内凹弧线

四角是**以矩形顶点为圆心、向内容内侧挖入的四分之一圆弧**，不是普通的外凸 `border-radius`。直线在距顶点一个半径处结束，再沿内凹圆弧转向下一条边。四角镜像对称；缩放时重新计算路径，禁止将整张边框图不等比拉伸。

标准半径：Canvas 卡片 11–13 逻辑 px；DOM 卡片 14–16 CSS px；大面板 22–24 CSS px。每层路径半径为 `max(2, min(radius - inset*.45, width/4, height/4))`。

从外到内画五道金属边沿：

| 内缩量 | 线宽 | 颜色 |
| --- | --- | --- |
| .60 | 2.70 | 暗金 `rgba(137,111,61,.60)` |
| 1.05 | 1.65 | 多色香槟金渐变 |
| 2.35 | .65 | 象牙白 `#fff8d8` |
| 4.10 | .70 | `rgba(215,188,122,.92)` |
| 6.30 | .55 | `rgba(255,253,226,.8)` |

四角内侧添加弧形细线和小菱形；大面板可以加枝叶曲线。顶边中心徽记由暗金、浅金、蓝色、象牙白的嵌套菱形组成，两侧小卷叶镜像。小卡片避免过多枝叶挤压文字。装饰位于内容后方，不接收点击，不进入无障碍树。

## 4. 六边形按钮与交互

左右尖端、上下平边的长六边形。斜肩长度为按钮高度的 `.40`；金属边沿、象牙白反光、蓝色或金色内芯逐层内缩绘制。底部中心有柔和径向反光，四个斜肩有弧形小高光，两端有对称小菱形。

- 主要动作（开始冒险、确认选择）：金色，默认保留轻微暖光。
- 返回、关闭、次要入口：蓝色，横竖屏一致。
- Hover 与键盘聚焦：轮廓增加约 2.1 px 金色亮边、1.25 px 近白内线、10–15 px 局部金色辉光，不能仅改变文字或填充。
- 选中：更亮更厚的金边、柔和金底、明确的勾选/状态标签，不能只依赖色相。
- 未选中角色：立绘覆盖少量灰蓝，文字仍然清晰。未解锁成就只降低图标饱和度，不能把整张卡片和正文一起压暗。
- 支持鼠标、触摸和键盘；减少动态效果偏好下停用装饰动画。

## 5. 布局基准

**主界面**：动态天空背景，上方透明金蓝标题，中下部金色开始按钮，下方蓝色次要按钮组。留出人物区域，保留现有标题和人物遮挡关系，无最外圈边框。

**职业选择横屏**：左侧三张内凹金边立绘卡，右侧职业简介；下方中央金色确认，右侧蓝色返回；选中卡有“已选择”。实际职业为剑士、琴师、术士。

**职业选择竖屏**：上方大立绘、中间三个职业头像、下方蓝白简介面板，金色确认与蓝色返回。行走图使用配置原四向素材播放朝下行走帧。职业简介不显示“定位”行。

**成就页**：浅色遮罩保留背景气氛；中心大金框、宋体标题、蓝色返回；四项进度统计；宽屏双列成就卡，窄屏单列。内容内部滚动，外框、标题和返回保持固定。

**成就大图弹框**：同源内凹金框、蓝白面板、大图、成就名与等级、蓝色返回。**解锁提示**：右下角小型珍珠金框、金色“成就已解锁”、图标、等级与名称，保留自动退场和排队行为。

## 6. 直接复用公共组件

公共对象：`fantasyUI_6f31b8ea_7c4d_4b67_a215_03b247f8e903`，由 `main.pureData` 加载，所有插件开始安装前可用。

```js
var theme = fantasyUI_6f31b8ea_7c4d_4b67_a215_03b247f8e903;

// Canvas：与职业选择、标题按钮使用相同实现。
theme.drawArcFrameOn(ctx, {x: 20, y: 30, w: 380, h: 260}, {
  radius: 13, fill: theme.pearlGradient(ctx, {y: 30, h: 260}), crest: true
});
theme.drawHexButtonOn(ctx, {x: 140, y: 300, w: 160, h: 40},
  "确认选择", true /* 金色 */, hovered, 18);

// DOM：尺寸由页面 CSS 控制，内容仍为原生文字/按钮。
theme.decorate(panelElement, {radius: 24, ornate: true, crest: true});
theme.decorate(cardElement, {radius: 15, interactive: true, selected: unlocked,
  fill: unlocked ? "gold" : "pearl"});
theme.decorate(backButton, {button: true, gold: false});

// 移除弹框或重建列表前释放装饰，避免遗留观察器/事件监听。
theme.releaseTree(panelElement);
panelElement.remove();
```

DOM 装饰用透明 Canvas，按元素实际尺寸与设备像素比重绘，复用原始内凹弧框与六边形函数。通过 `ResizeObserver` 响应尺寸变化，无常驻绘制循环。先将元素挂入文档再装饰；正文留 16–24 px 内边距，徽记上方留 10 px。不要给装饰 Canvas 加 `position:relative`。

按钮装饰画布在四周额外扩展 24 CSS px，并同步扩大实际位图、平移绘制原点，为高亮外发光留出空间。按钮自身使用 `overflow:visible`；仅修改 CSS overflow 无法解决 Canvas 位图边缘裁切。扩展的装饰不接收点击，按钮本体尺寸与点击区域不变；滚动列表中的卡片不扩展，以免改变滚动范围。

成就页顶部四项统计同时作为等级筛选按钮：全部、青铜、白金、钻石。选中项使用暖金底、金色亮边和 `aria-pressed`；其余为珍珠底。固定筛选卡调用 `decorate` 时设置 `interactive: true, bleed: 20`，完整保留悬停辉光。切换分类后列表回到顶部，统计仍显示各等级的完整解锁进度；再次打开页面默认显示全部。

## 7. 行为与验收约束

主界面与职业选择复用同一 MP4 节点，切换时不调用 `load()`、不归零 `currentTime`、不重新挂载。现有轻雾渐变顶部透明度 .04、中部 .12、底部 .47，不重新加深黑色遮罩。

只改表现层；保留成就定义、隐藏条件、解锁时间、存储键、音效、排队、游戏控制锁和焦点恢复。定制放在 `project/`，不修改 `libs/`。

每次扩展检查横屏、竖屏、小屏；内凹弧角与完整金属层次；文字不挤入装饰区；hover 金边高亮；选中/未选中明显；列表可滚动、弹框可关闭；动态背景持续播放。

后续需求可直接写：**“遵循 UI美术规范.md，复用 project/fantasyUI.js 改造某页面。”**

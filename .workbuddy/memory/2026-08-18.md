# 2026-08-18

## push 失败（网络阻断）
- commit eef8539（商店/武器/楼层配置调整）成功，工作区干净。
- push 到 origin/dev/51_gbf_ailuda 失败：github.com:443 无法连接（ping 通、443 被阻断；常见代理端口 7890/10809/1080/8888/7897 均不可用）。
- 待网络恢复/用户提供代理后重推：git push origin dev/51_gbf:dev/51_gbf_ailuda

## 应用 ailuda 参考工程的显伤种子确定性与录像确定性
- 对比参考工程 mota_mod-dev-51_gbf_ailuda（2.10.53 独立分支）发现真实差异：
  - 显伤：ailuda 用"种子确定性随机"（createSeededRandom 复刻 Park-Miller，按实际战斗调用顺序掷骰），当前是"期望值版"。
  - 录像：ailuda 实例 ID 用 flag 计数器（确定性可复现）；当前是 Date.now 时间戳。
- 应用（当前工程）：
  1. 显伤：backpackBattleEstimateKernel.js/Estimate.js/Worker.js 整体替换为 ailuda 种子版；backpackBattle.js 加 randomSeed 传递（core.getFlag("__rand__")）+ BATTLE_RULE_VERSION 3→4；rules.js nearbyCleanse 走 handlers 随机。
  2. 录像：backpackSystem.js makeInstanceId 改用 __backpack_instance_id__ flag 自增计数器；payload 括号转义当前已具备。
- 测试更新：#22/#23 改种子确定性断言（seed123 damage=280/rounds15；seed456 rounds=2 damage=0）、#43 禁 token 改为调用类（允许注释提及 __rand__）、#54 正则改 random.pick(pool)。
- 89 项测试全过。version 2.10.54。备份在 tmp/ailuda-backup/。

## 商店/赠予选择器录像改为 choices:n 模拟操作（对齐 ailuda）
- 用户指出：之前每次购买/选择 push 完整背包快照（backpack:%7B...%7D）臃肿重复；ailuda 是 choices:n（模拟操作选择）。
- 改造（backpackShop.js）：
  - 新增 pushShopChoice/rollbackShopChoice：正常游戏记录 core.status.route.push("choices:"+index)（0=刷新、1~5=购买/选择），失败撤销。
  - doRefresh/buyWeapon 支持 options{recordChoice,silent}；applyShopChoice/applyRewardChoice 统一入口（正常点击与回放共用）。
  - buildCard 加 choiceIndex；购买按钮走 applyShopChoice(index+1)；赠予回调走 applyRewardChoice。
  - 移除 pushBackpackReplay（快照式）；grantWeapon 兜底 ID 改用 __backpack_instance_id__ 计数器。
  - 回放：openShop/openRewardPicker 在 isReplayingNow 时不建 DOM，加载货架/生成候选并设 replayChoiceMode（shop/reward）；registerReplayAction("backpackShopChoice") 解析 choices:n → 执行（recordChoice:false, silent:true）→ finish（route.push+replay）/fail（_replay_error）；处理器优先级提到 ignoreInput 之前。
- 89 项测试全过。version 2.10.55。

## 新效果 modifyBattleMaxHp：命中时自身最大HP+10
- 需求：攻击命中时自身最大HP+10；"最大HP"=进入战斗时的最大生命值（battleMaxHp，hpPercent/回血上限的分母），不是样板 hpmax。
- 实现（backpackBattleRules.js effect 分派）：modifyBattleMaxHp → state.battleMaxHp += value 且 state.player.maxHp 同步 +=（当前 hp 不补）；appendLog。
- 验证：命中1次 → battleMaxHp 800→810、player.maxHp 810、当前 hp 800 不变。89 项测试全过。version 2.10.56。
- 配置示例：combatRules [{trigger:"afterHit", effects:[{type:"modifyBattleMaxHp", value:10}]}]

## 同步规则说明文档
- 武器布局联动与战斗触发规则说明.md 补充：sameNameCount groupBy（任意同名组/weaponTypes）、debuffStacks 条件小节（含"弱体10净化回血饮料"示例）、modifyBattleMaxHp 效果行。

## 新效果 nearbyMaxHpBonus：附近每1个乐器/动物 → 最大HP+3
- 需求：战斗开始自身再生+1（现成）；上下左右1格内每配置1个乐器或动物，自身最大HP+3（battleMaxHp 语义）。
- 实现（backpackBattleRules.js）：nearbyMaxHpBonus → countNearbyWeapons/every → battleMaxHp += count*value 且 player.maxHp 同步（当前 hp 不补）；filter.weaponTypes 多类型"任一匹配"（matchesFilter 用 some）✓。
- 验证：自身+上方乐器+下方动物（2件）→ battleMaxHp 800→806、regen 1。89 项测试全过。version 2.10.57。
- 配置示例：battleStart → applyStatus regeneration 1 + nearbyMaxHpBonus{directions 上下左右, distance 1, filter {weaponTypes:[乐器,动物]}, value 3}。
- 文档已补 nearbyMaxHpBonus 行。

## 格挡改为每层抵挡3点（不足3点也消耗1层）
- 需求：每层格挡抵挡3点伤害；只造成1点伤害也会消耗1层。
- 实现（backpackBattleRules.js applyDamage）：blockPerLayer=3；blocksNeeded=min(层数, ceil(伤害/3))；blocked=min(剩余伤害, blocksNeeded×3)；按 blocksNeeded 消耗层数。
- 验证：伤害1→挡1耗1层；2→挡2；3→挡3；4→挡4耗2层；7→挡7耗3层；8→挡8耗3层（全实伤0）。
- 注意：main.js 版本多次被回退到旧值（疑似编辑器覆盖），已重新 bump 2.10.59。89 项测试全过。

## 格挡测试断言更新
- #15：block15 伤害10×2 → 每次耗 ceil(10/3)=4 层 → 剩 11/7（新语义）。
- #64 场景2：block5 伤害10（非 ignoreBlock）→ 耗 4 层 → blockLeft 1、实伤 0（旧 0/5）。
- #69：block18 伤害15（makeW 默认）→ 耗 ceil(15/3)=5 层 → 18-5-8(removeStatus)=5。
- 89 项测试全过。version 2.10.59。

## 格挡改动导致的随机驱散断言更新（#69）
- 新格挡下 block 消耗减少 → 随机驱散池变化（block 剩 5 在池中）→ 反射 12-8 后未被随机驱散选中 → 剩 4（旧 3）。
- 89 项测试全过。version 2.10.59。

## 再生改为每2秒触发/掉2层/每层回5
- 需求：再生每 2 秒触发一次，触发时掉 2 层，按层数回血每层 5。
- 实现：
  - backpackBattleStatuses.js：regeneration periodTicks 100→200（UI 描述）。
  - backpackBattleRules.js settlePeriodicStatuses：再生分支加 state.tick % 200 === 0（周期结算每 100 tick 调用，这里按偶数秒过滤）；heal ×5；removeStatusStacks 2。
  - 注意：settlePeriodicStatuses 固定每 100 tick 调用（periodTicks 不控制频率），所以用 tick%200 过滤实现"每2秒"。
- 验证：4层再生 → tick100 不触发(500/4层)、tick200 回20掉2层(520/2层)、tick400 回10掉光(530/0层)。89 项测试全过。version 2.10.60。

## 再生测试更新
- #17：tick100 再生不触发（hp880 只烧）；tick200 烧伤再-20+再生+15=875、掉2层剩1。
- #83：tick100 再生未触发 → 535（500+15+20）；tick200 再生+15 → 550；once 激奏不重复。
- 89 项测试全过。version 2.10.60。

## 修复：nearbyApplyStatus / nearbyMaxHpBonus 不显示联动范围
- 症状：battleStart 里 nearbyApplyStatus/nearbyMaxHpBonus（sideBox span3 上下区域）不显示联动范围。
- 根因：collectSpatialRules effects 白名单缺这两类。
- 修复：白名单加 nearbyApplyStatus、nearbyMaxHpBonus。
- 验证：用户规则 → 联动格 6（上/下 sideBox span3 各3格：[[-1,-1],[0,-1],[1,-1],[-1,1],[0,1],[1,1]]）。89 项测试全过。version 2.10.61。

## 同步格挡/再生 hover 描述
- 需求：格挡（每层挡3点）与再生（每2秒/掉2层/每层回5）改了数值，但战斗中 buff 悬停描述没改。
- 实现（backpackBattleStatuses.js description 字段）：
  - block：'每层抵挡 3 点税前伤害，当前最多可挡 N 点（不足 3 点的伤害也会消耗 1 层）'。
  - regeneration：'每 2 秒触发一次：恢复 N HP（每层回 5），随后变为 M 层'。
- UI（backpackBattleUI.js）通过 definition.description() 动态调用，改 description 即同步。
- 89 项测试全过。version 2.10.62。

## 新效果 nearbyDamageSelf：上方/下方横向三格每1个武器 → 自身HP-50
- 需求：战斗开始时，上方横向三格和下方横向三格内每配置1个武器，自身HP-50。
- 实现（backpackBattleRules.js）：nearbyDamageSelf → countNearbyWeapons/every → state.player.hp -= count*value（直接扣血，不经过格挡/防御）+ appendLog。
- 联动范围白名单加 nearbyDamageSelf。
- 验证：上/下 sideBox span3 区域内 2 把武器 → HP 1000→900。89 项测试全过。version 2.10.63。文档已补。
- 配置：battleStart → nearbyDamageSelf{value 50, directions [up,down], distance 1, relation sideBox, span 3, filter {}, every 1}
